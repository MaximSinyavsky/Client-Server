# Client-server theory
## 1.	Что такое клиент? Приведи пример, что может быть клиентом.
Клиент - это устройство на стороне пользователя, которое отправляет запрос к серверу для предоставления информации или выполнения определенных действий.

📌Примеры:
* Веб-браузер: Chrome, Firefox, Safari.
* Почтовый клиент: Outlook, Thunderbird.
* Мобильные приложения: WhatsApp, Instagram.
* Компьютерные игры: Клиентские приложения игр, такие как World of Warcraft или Fortnite, которые подключаются к игровым серверам.

## 2.	Что такое сервер? Приведи пример, что может быть сервером сервера.
Сервер — это компьютер или система, которая предоставляет ресурсы, данные или услуги другим компьютерам (клиентам) в сети. Серверы обрабатывают запросы от клиентов и отправляют соответствующие ответы.

📌Примеры:
* Веб-сервер: Например, Apache или Nginx, который обслуживает веб-страницы.
* Почтовый сервер: Например, Microsoft Exchange, который управляет электронной почтой.
* Базы данных сервер: Например, MySQL, который хранит и управляет данными.

## 3.	Что такое Interface? Приведи пример.
Интерфейс — это точка взаимодействия или способ общения между двумя системами, компонентами, программами или устройствами. Интерфейсы определяют правила, по которым одна часть системы может взаимодействовать с другой, позволяя им обмениваться данными или командами

📌Примеры:
1. API (Application Programming Interface): это интерфейс, который позволяет различным программам взаимодействовать друг с другом.
2. REST API (Representational State Transfer):
* Использует HTTP и работает через URL.
* Примеры: GitHub API, Google Maps API.
3. SOAP API (Simple Object Access Protocol):
* Основан на XML и используется для обмена данными через HTTP и другие протоколы.
* Примеры: банковские системы, корпоративные интеграции.
4. GraphQL API:
* Позволяет клиентам запрашивать конкретные данные.
* Разработан Facebook.
* Примеры: GitHub GraphQL API.
5. RPC API (Remote Procedure Call):
* Клиент вызывает функции или процедуры на удаленном сервере.
* Примеры: gRPC (Google Remote Procedure Call).
6. WebSocket API:
* Поддерживает постоянное соединение для обмена данными в реальном времени.
* Примеры: чаты, онлайн-игры.
7. Streaming API:
* Позволяет получать данные в реальном времени.

## 4.	 Что такое толстый клиент? Приведи реальный пример, когда клиент считается толстым.
Толстый клиент — это клиентская программа или приложение, которое выполняет большинство или все операции локально на устройстве пользователя, а не на сервере. Такие приложения обычно требуют установки и могут работать автономно или с периодическим подключением к серверу для синхронизации данных.

📌Примеры:
* Microsoft Outlook.
* Telegram
* Онлайн игры

## 5.	Что  такое тонкий клиент? Приведи реальный пример, когда клиент считается тонким.
Тонкий клиент — это устройство или программа, которое выполняет минимум операций локально и в основном полагается на сервер для обработки данных и выполнения задач. Тонкий клиент чаще всего используется в корпоративных системах, где важны централизованное управление и обслуживание.

📌Примеры:
* Веб-браузеры: Google Chrome, Mozilla Firefox — они полагаются на серверы для загрузки и отображения веб-страниц.
* Zoom: Видеоконференции проходят через серверы Zoom, а ваш компьютер лишь отображает и отправляет видео.

## 6.	Какие бывают уровни клиент серверной архитектуры?
### Клиент-серверная архитектура обычно включает несколько уровней:
* Одноуровневая архитектура: Клиент напрямую взаимодействует с сервером. Простейший случай, часто встречающийся в небольших приложениях.
* Двухуровневая архитектура: Клиент и сервер разделены на два уровня. Клиентский уровень отвечает за пользовательский интерфейс, а серверный — за обработку данных и логику приложения.
* Трехуровневая архитектура: Включает три уровня — клиентский, сервер приложений (где выполняется бизнес-логика) и сервер данных (БД). Часто используется в крупных корпоративных приложениях.
* Многоуровневая архитектура: Включает более трех уровней. Например, может быть уровень веб-сервера, сервер приложений, сервер базы данных и уровни для внешних сервисов и API. Такая архитектура обеспечивает лучшую масштабируемость и распределение нагрузки.

## 7.	Что такое балансировщик нагрузки (балансировщики)?
Балансировщик нагрузки (англ. Load Balancer) — это программное или аппаратное устройство, которое распределяет входящий трафик или запросы между несколькими серверами для обеспечения высокой доступности, устойчивости и оптимальной производительности системы. 
Балансировщик может быть размещен до (на фронтэнде) или после (на бэкенде) серверов в зависимости от архитектуры системы.
### Основные задачи балансировщика нагрузки:
* Распределение нагрузки — балансировщик направляет запросы пользователей на разные серверы в зависимости от текущей загруженности каждого сервера.
* Обеспечение отказоустойчивости — если один сервер выходит из строя, балансировщик автоматически перенаправляет запросы на другие доступные серверы, что помогает избежать простоев.
* Улучшение производительности — за счёт распределения нагрузки между серверами система может обрабатывать больше запросов одновременно, обеспечивая высокую производительность.
* Масштабируемость — позволяет легко добавлять новые серверы в систему без необходимости изменять архитектуру приложения, что делает возможным горизонтальное масштабирование.
### Виды:
* программные
* аппаратные
* облачные

## 8.	Горячий резерв серверов? Холодный резерв серверов? Приведи реальный пример.
### Горячий резерв серверов (Hot Standby)
Это ситуация, когда резервные серверы находятся в активном режиме и постоянно синхронизированы с основными серверами. Если основной сервер выходит из строя, резервный сервер (или несколько) немедленно берет на себя работу без существенного перерыва в обслуживании. Горячий резерв обеспечивает минимальное время переключения (практически мгновенное).

📌Пример: 
В инфраструктуре облачных сервисов, таких как Amazon Web Services (AWS), можно настроить автоматическое переключение на резервный сервер. Если основной сервер выходит из строя, горячий резерв моментально берет на себя работу, минимизируя простой системы.

### Холодный резерв серверов (Cold Standby)
Это ситуация, когда резервные серверы не активны, но готовы к включению в случае сбоя основного сервера. В случае отказа основного сервера нужно вручную или автоматически активировать резервный сервер, загрузить его и перенести на него необходимые данные. Этот процесс может занять некоторое время, так как серверы не синхронизируются постоянно с основными серверами.

## 9.	Чем web service отличается от web server? Приведи пример.
### Веб-сервер
— это программное обеспечение или аппаратное устройство, которое принимает запросы от клиентов (обычно через браузер) по протоколу HTTP/HTTPS и отвечает на эти запросы, отправляя клиенту веб-страницы или другие ресурсы (файлы, изображения и т.д.).
### Веб-сервис
— это программный компонент или приложение, которое позволяет другим программам обмениваться данными через интернет с использованием стандартных протоколов, таких как HTTP, SOAP или REST. В отличие от веб-сервера, веб-сервис не предназначен для взаимодействия с пользователями напрямую через браузер — его цель состоит в обмене данными между приложениями.

📌Пример: вы разрабатываете мобильное приложение, которое должно отображать текущую погоду. Это приложение обращается к веб-сервису (например, OpenWeather API), отправляя HTTP-запрос с нужными параметрами (например, городом), и веб-сервис возвращает JSON-ответ с данными о погоде.

Web server может обслуживать web service. Например, когда клиентское приложение отправляет запрос к API, этот запрос обрабатывается веб-сервером, который направляет его на веб-сервис, а затем возвращает ответ клиенту.
Таким образом, веб-сервер — это инфраструктурный компонент, который обрабатывает HTTP-запросы и доставляет контент, а веб-сервис — это функциональный компонент, который предоставляет данные или услуги другим программам через интернет.


## 10.	Что такое микросервисная архитектура? Приведи реальный пример использования.
Микросервисная архитектура — это стиль разработки программного обеспечения, в котором приложение делится на небольшие, самостоятельные сервисы. Каждый микросервис выполняет конкретную задачу и может быть разработан и развернут независимо от других. Это позволяет улучшить масштабируемость и гибкость системы.

📌Пример: 

Spotify использует микросервисную архитектуру для обеспечения работы своей платформы. Каждый микросервис в Spotify отвечает за определенный аспект системы, такой как:
* Каталог музыки: Управление и хранение информации о миллионах треков.
* Рекомендации: Генерация персонализированных рекомендаций на основе истории прослушиваний.
* Плейлисты: Управление пользовательскими плейлистами и их синхронизация.
* Потоковая передача музыки: Обеспечение стабильного и качественного стриминга аудио.

## 11.	Прокси сервер? Приведи пример.
Прокси-сервер (от англ. proxy — «представитель» или «посредник») — это сервер, который выступает посредником между клиентом и целевым сервером, к которому клиент хочет подключиться. Прокси-сервер принимает запросы от клиента, пересылает их на целевой сервер, получает ответ от этого сервера и возвращает его обратно клиенту. 
### Основные функции и задачи прокси-сервера:
* Анонимизация: Прокси-сервер скрывает настоящий IP-адрес клиента, заменяя его своим, что позволяет пользователю оставаться анонимным в интернете.
* Кэширование: Прокси-сервер может кэшировать часто запрашиваемые ресурсы (например, веб-страницы или изображения) и предоставлять их пользователям быстрее, не обращаясь каждый раз к целевому серверу.
* Контроль доступа: Прокси-сервер может использоваться для фильтрации контента, блокировки определённых сайтов или управления доступом пользователей к ресурсам интернета.
* Оптимизация трафика: Прокси может сжимать данные, что уменьшает объём передаваемого трафика и ускоряет работу в сети.
* Безопасность: Прокси-сервер может выполнять проверку запросов, фильтрацию подозрительных соединений и защиту от вредоносных сайтов.
* Балансировка нагрузки: Прокси-сервер может распределять запросы пользователей между несколькими серверами для оптимизации нагрузки.
### Для чего нужен proxy:
* обеспечивает анонимность — прокси меняет IP-адрес, поэтому сторонним лицам сложнее отследить местоположение пользователя;
* блокирует сайты — можно настроить его так, чтобы ограничить доступ к определенным ресурсам, например, социальным сетям в офисе;
* ускоряет работу — за счет кэширования данных страницы загружаются быстрее;
* фильтрует контент — прокси позволяет отсеивать нежелательную рекламу и опасные сайты.
### Прокси-сервер также можно использовать для бизнес-задач:
* контролировать трафик — компании могут отслеживать запросы сотрудников и анализировать их активность в интернете;
* повышать безопасность — он защищает от утечек данных, делая взаимодействие с сетью более безопасным;
* оптимизировать рекламу — с его помощью можно собирать данные о предпочтениях пользователей и настраивать рекламу точнее.
### Типы прокси-серверов:
* Прямой (обычный) прокси: Используется для передачи запросов от клиента к целевому серверу.

📌Пример: Пользователь подключается к веб-сайту через прокси, чтобы скрыть свой реальный IP-адрес.
* Обратный прокси (Reverse Proxy): В отличие от обычного прокси, обратный прокси расположен на стороне сервера и используется для перераспределения входящих запросов к разным серверам внутри сети.

📌Пример: Веб-сайты, такие как YouTube или Google, могут использовать обратные прокси для балансировки нагрузки между несколькими серверами, что помогает справляться с огромным количеством запросов.

## 12.	 VPN? Приведи пример.
VPN (Virtual Private Network) — это технология, которая создает защищенное и зашифрованное соединение между вашим устройством и интернетом. VPN используется для защиты личных данных и обеспечения приватности в сети.
1. Безопасность и приватность:
* Использование: Шифрование данных для защиты при использовании общественных Wi-Fi сетей в кафе или аэропортах.
* 📌Пример: При подключении к общедоступному Wi-Fi в кафе через VPN, ваши данные становятся защищёнными и недоступными для хакеров.
2. Удалённая работа:
* Использование: Обеспечение безопасного доступа сотрудников к корпоративной сети из дома.
* 📌Пример: Сотрудники компании Google используют VPN для безопасного доступа к внутренним ресурсам компании, находясь за пределами офиса.
3. Обход геоограничений:
* Использование: Доступ к контенту, который заблокирован в вашем регионе.
* 📌Пример: Если вы хотите посмотреть сериалы на американском Netflix, находясь в другой стране, вы можете использовать VPN-сервис, например, ExpressVPN, чтобы обойти геоограничения.
4. Защита конфиденциальности:
* Использование: Сокрытие вашего реального IP-адреса для повышения анонимности в интернете.
* 📌Пример: Журналисты и активисты используют VPN для безопасного общения и доступа к информации без отслеживания.

## 13.	Что такое атака MAN in the Midl?

Атака "Man-in-the-Middle" (MITM) — это тип кибератаки, при которой злоумышленник тайно перехватывает и изменяет коммуникацию между двумя сторонами, которые думают, что общаются напрямую друг с другом.

📌Пример: Представь, что ты отправляешь сообщение другу. Злоумышленник перехватывает это сообщение, изменяет его и отправляет другу, при этом оба вы думаете, что общаетесь напрямую. Злоумышленник может получить доступ к конфиденциальной информации, такой как пароли или банковские данные.

## 14.	Что такое API?

API (Application Programming Interface): это интерфейс, который позволяет различным программам взаимодействовать друг с другом.
1. REST API (Representational State Transfer):
* Использует HTTP и работает через URL.
* Примеры: GitHub API, Google Maps API.
2. SOAP API (Simple Object Access Protocol):
* Основан на XML и используется для обмена данными через HTTP и другие протоколы.
* Примеры: банковские системы, корпоративные интеграции.
3. GraphQL API:
* Позволяет клиентам запрашивать конкретные данные.
* Разработан Facebook.
* Примеры: GitHub GraphQL API.
4. RPC API (Remote Procedure Call):
* Клиент вызывает функции или процедуры на удаленном сервере.
* Примеры: gRPC (Google Remote Procedure Call).
5. WebSocket API:
* Поддерживает постоянное соединение для обмена данными в реальном времени.
* Примеры: чаты, онлайн-игры.
6. Streaming API:
* Позволяет получать данные в реальном времени.

## 15.	Что такое REST API?
REST API (Representational State Transfer API) — это архитектурный стиль для создания веб-сервисов, которые могут взаимодействовать через HTTP-протокол. REST API позволяют различным приложениям обмениваться данными и выполнять операции, используя стандартные HTTP-методы: GET, POST, PUT, DELETE и др.

Простыми словами, REST API позволяют получать доступ к ресурсам сервера (данные, функции и т.д.) через URL-адреса, отправляя запросы и получая ответы в формате JSON или XML. Они широко используются благодаря своей простоте и гибкости, и помогают обеспечить легкую интеграцию между различными системами.

## 16.	Требования к архитектуре Rest.
Принципы проектирования REST API (Если выполняются все принципы, то RESful)
Теперь, когда мы рассмотрели основы и узнали об определении REST API, давайте перейдем к шести принципам REST, которые определяют дизайн API:

* Клиент-сервер

Принцип REST основан на концепции, согласно которой клиент и сервер должны быть изолированы друг от друга и иметь возможность развиваться независимо. Таким образом, вы можете улучшить управляемость на многочисленных платформах и увеличить масштабируемость за счет оптимизации серверных компонентов, поскольку проблемы пользовательского интерфейса отделены от проблем хранения данных.

* Stateless

Этот принцип REST требует, чтобы API не сохраняли состояние, что позволяет выполнять независимые вызовы. Более того, каждый вызов включает в себя данные, необходимые для его эффективного завершения. Другими словами, каждый запрос, отправляемый клиентом на сервер, должен включать всю информацию, необходимую для обработки запроса.

* Кэшируемый

Поскольку API без сохранения состояния может увеличить накладные расходы на запросы, управляя огромными нагрузками входящих и исходящих вызовов, дизайн REST API должен хранить кэшируемые данные. Согласно этому принципу проектирования API, данные в ответе должны быть косвенно или классифицированы как кэшируемые или некэшируемые. Если ответ кэшируется, кэшу клиента предоставляется право повторно использовать эти данные ответа для аналогичных запросов в будущем.

* Единый интерфейс

Чтобы отделить клиент от сервера, вам необходимо иметь единый интерфейс, который позволяет автономно разрабатывать приложение без жесткой привязки его сервисов, моделей и действий к самому уровню API. Этот принцип проектирования оптимизирует всю архитектуру системы и повышает наглядность. коммуникаций.Некоторые архитектурные элементы управления требуют управления производительностью элементов в архитектуре REST API для достижения единообразного интерфейса. Архитектура REST API определяет принципы REST с помощью четырех элементов управления интерфейсом, включая идентификацию ресурсов, управление ресурсами посредством представлений, обеспечение самоописательной связи и превращение гипермедиа в механизм состояния приложения.

* Многоуровневая система

Архитектура REST API включает в себя несколько уровней, которые работают вместе, создавая иерархию, помогающую создать более масштабируемое и гибкое приложение. Благодаря многоуровневой системе приложение имеет более высокий уровень безопасности, поскольку компоненты каждого уровня не могут взаимодействовать за пределами последующего уровня. Более того, он балансирует нагрузки и предлагает общие кэши для стимулирования Масштабируемость. Система многоуровневой архитектуры REST API обладает большей стабильностью, поскольку ограничивает производительность компонентов. так что каждый компонент не может «видеть» дальше непосредственного слоя, с которым он смешивается.

* Код по требованию

Принцип REST обеспечивает передачу кода или апплетов через API, используемый внутри приложения. Определение REST API позволяет расширять функциональность клиента путем загрузки и реализации кода в форме апплетов или сценариев. Это оптимизирует работу клиентов за счет уменьшения количества функций, которые необходимо предварительно реализовать. Большую часть времени сервер возвращает статическое представление ресурса в формате XML или JSON. Но при необходимости серверы могут доставить клиенту исполняемый код.

## 17.	 Что такое CRUD?

CRUD — это аббревиатура, которая обозначает четыре основных операции, выполняемых с базами данных или другими хранилищами данных. Эти операции используются для управления данными в приложениях и сервисах.
Create - Read - Update - Delete

## 18.	Чем GET отличается от POST?
### GET:
* Цель: Основная цель метода GET — получение данных с сервера. Он используется для запроса информации, которая не изменяет состояние сервера.
* Передача данных: Данные передаются в URL. Например, при запросе https://example.com/search?q=example параметры передаются в строке запроса (query string).
* Безопасность: GET-запросы менее безопасны, так как данные видны в URL и могут быть закэшированы в браузере или на прокси-сервере. Это делает GET-запросы уязвимыми для подглядывания и записей в истории браузера.
* Идпотентность: GET-запросы считаются идемпотентными, что означает, что несколько идентичных запросов дадут один и тот же результат и не изменят состояние сервера.
* Ограничения: Из-за ограничения длины URL в некоторых браузерах и серверах (обычно около 2000 символов), GET-запросы подходят только для небольших объемов данных.
### 📌Применение:
* Поиск информации.
* Загрузка веб-страниц.
* Получение данных через API.
### POST:
* Цель: Основная цель метода POST — отправка данных на сервер для создания или обновления ресурса. Он используется для операций, которые могут изменить состояние сервера или создать новый ресурс.
* Передача данных: Данные передаются в теле запроса. Например, при отправке формы регистрации данные передаются в теле HTTP-запроса.
* Безопасность: POST-запросы считаются более безопасными, так как данные не видны в URL и не закэшируются так же легко. Тем не менее, они всё равно должны быть защищены с помощью HTTPS.
* Идпотентность: POST-запросы не являются идемпотентными. Каждый новый POST-запрос может изменить состояние сервера или создать новый ресурс.
* Ограничения: POST-запросы не имеют ограничений по объему данных, так как данные передаются в теле запроса.
### 📌Применение:
* Отправка форм (например, формы регистрации).
* Загрузка файлов.
* Передача больших объемов данных через API.

## 19.	Чем POST отличается от PUT?
### POST:
* Цель: Создание нового ресурса на сервере.
* Передача данных: Данные передаются в теле запроса.
* Идпотентность: Не является идемпотентным. Каждый новый POST-запрос может создать новый ресурс. Повторные запросы могут привести к созданию дубликатов.
* 📌Пример: Отправка формы регистрации пользователя. Каждый раз, когда отправляется POST-запрос, создаётся новый пользователь.
### PUT:
* Цель: Создание или обновление ресурса на сервере.
* Передача данных: Данные передаются в теле запроса и обычно содержат все данные ресурса, которые необходимо создать или обновить.
* Идпотентность: Является идемпотентным. Повторные PUT-запросы с одинаковыми данными не изменят состояние сервера после первого запроса.
* 📌Пример: Обновление информации о пользователе. Если PUT-запрос отправляется с одними и теми же данными несколько раз, состояние ресурса не изменится после первого запроса.
### Краткое сравнение:
* POST используется для создания новых ресурсов, и каждый запрос может создавать новые дубликаты.
* PUT используется для создания или обновления ресурса, и повторные запросы не изменяют состояние ресурса после первого успешного запроса.

## 20.	Чем PUT отличается от PATCH?
### PUT:
* Цель: Обновление или замена всего ресурса. Если ресурс не существует, то он создаётся.
* Передача данных: В теле запроса передаются все данные ресурса.
* Идпотентность: PUT-запросы являются идемпотентными, что означает, что повторные запросы не изменят состояние ресурса после первого запроса.
* 📌Пример: Если обновляется информация о пользователе, PUT-запрос заменяет весь профиль пользователя на новые данные.
### PATCH:
* Цель: Частичное обновление ресурса. Вносит изменения только в указанные поля ресурса.
* Передача данных: В теле запроса передаются только те данные, которые необходимо обновить.
* Идпотентность: PATCH-запросы также могут быть идемпотентными, но это зависит от реализации. В целом, они предназначены для частичных изменений и могут не быть идемпотентными в некоторых случаях.
* 📌Пример: Если нужно обновить только адрес пользователя, PATCH-запрос изменит только поле адреса, не затрагивая остальную часть профиля.

Так что PUT заменяет весь ресурс, а PATCH обновляет только определённые части ресурса.

## 21.	Можно ли в POST передать данные и через URL и через Body?
Да, в POST-запросе данные могут быть переданы как через URL (в строке запроса), так и через тело запроса (body). Однако это не является обычной практикой и может запутать обработку запроса на сервере
Рекомендуется:
* Передавать параметры, которые не меняют состояние сервера (например, фильтры, ключи), через URL.
* Передавать данные, которые создают или изменяют ресурс, через тело запроса.
### 📌Пример POST-запроса с данными в теле:
    POST /api/resource
    {
      "name": "Example",
      "value": 123
    }
### 📌Передача данных через URL может выглядеть так:
    POST /api/resource?name=Example&value=123
## 22.	Можно ли с помощью URLa передать данные на сервер?
Передача данных на сервер через URL, это обычно делается с помощью строки запроса (query parameters). Строка запроса начинается после символа ? в URL и состоит из пар "ключ=значение", разделенных символом &.
### Структура запроса:
     GET /api/resource?key1=value1&key2=value2
Параметры: Параметры передаются в формате "ключ=значение". Например, name=John или age=30.
Разделение параметров: Каждый параметр разделяется символом &.

    📌Пример: URL для поиска пользователя по имени и возрасту:
    GET /api/users?name=John&age=30
    Здесь передаются два параметра: name и age.

    📌Пример: Фильтрация продуктов по категории и цене:
    GET /api/products?category=books&price=low
    Здесь передаются параметры category и price.

### Практическое использование:
* API-запросы: Очень часто используется в REST API для получения данных с сервера.
* Поиск на сайте: Передача поисковых параметров для получения результатов поиска.
* Фильтрация данных: Используется для фильтрации и сортировки данных на сайте.
### Однако есть некоторые ограничения:
* Ограничение длины URL: В некоторых браузерах и серверах существует ограничение на длину URL (обычно около 2000 символов).
* Безопасность: Данные, передаваемые через URL, видны в строке адреса и могут быть закэшированы или попасть в историю браузера, что может быть небезопасным для передачи чувствительных данных.
* Для больших объемов данных и передачи конфиденциальной информации лучше использовать методы POST, PUT или PATCH, где данные передаются в теле запроса.

## 23.	Что такое идемпотентность?
Идемпотентность — это важное свойство, которое гарантирует, что повторное выполнение одной и той же операции даст тот же результат, что и первоначальное выполнение. Это свойство критически важно для обеспечения устойчивости и предсказуемости систем, особенно в условиях, когда сетевые запросы могут быть повторены из-за сбоев.
###Основные аспекты идемпотентности:
### Определение:
* Идемпотентная операция — операция, которую можно выполнять многократно без изменения результата после первого выполнения.
* Пример: Умножение числа на 1 или прибавление 0 всегда даст исходное число, независимо от того, сколько раз эти операции выполняются.
### HTTP-методы:
* GET: Запрашивает данные. Повторные запросы не изменяют состояние сервера.
* PUT: Создаёт или обновляет ресурс. Повторные запросы с одинаковыми данными не изменяют ресурс после первого запроса.
* DELETE: Удаляет ресурс. Повторные запросы не изменят состояние, если ресурс уже удалён.
* POST: Не является идемпотентным, так как повторные запросы могут создать дубликаты ресурсов.
### Практическое значение:
* В распределённых системах и API идемпотентность упрощает обработку повторных запросов без изменения состояния системы.
* Для банковских переводов идемпотентность предотвращает многократное списание средств при повторных запросах.
### Примеры в реальной жизни:
* REST API: Если запрос GET на получение информации о пользователе повторяется, ответ всегда будет одинаковым, и состояние пользователя не изменится.
* Сетевые протоколы: Протоколы передачи данных могут использовать идемпотентные методы для повторной отправки пакетов, не изменяя целостность данных.
### Как это реализуется:
* Контроль версий и хэширование: Для идентификации изменений в данных и обеспечения согласованности запросов.
* Эффективные алгоритмы: Для обработки повторяющихся запросов без изменения состояния системы.
* Идемпотентность помогает построить надежные и масштабируемые системы, обеспечивая предсказуемость и устойчивость операций.

## 24.	В чем разница между безопасностью и идемпотентностью?
### Безопасность (Safety)
Безопасность в контексте HTTP-методов означает, что выполнение запроса не изменяет состояние сервера. Это своего рода гарантия, что запрос не внесёт никаких изменений в данные или ресурсы на сервере.
### Примеры безопасных методов:
* GET: Запрашивает данные. Сервер остаётся неизменным.
* HEAD: Похож на GET, но возвращает только заголовки ответа.
* OPTIONS: Позволяет узнать, какие методы поддерживаются сервером, без изменения состояния сервера.
### Идемпотентность (Idempotence)
Идемпотентность означает, что повторное выполнение одного и того же запроса приведёт к одному и тому же результату и состоянию сервера, что и одно выполнение этого запроса. Даже если запрос повторяется несколько раз, состояние сервера остаётся неизменным после первого успешного выполнения.
### Примеры идемпотентных методов:
* GET: Повторный запрос всегда возвращает один и тот же результат.
* PUT: Повторный запрос заменяет ресурс новыми данными, сохраняя консистентность.
* DELETE: Удаляет ресурс, и повторные запросы не изменяют состояние, если ресурс уже удалён.
### Основные различия:
### Изменение состояния сервера:
* Безопасность: Методы не изменяют состояние сервера.
* Идемпотентность: Методы могут изменить состояние сервера, но повторные запросы не меняют состояние после первого.
### 📌Примеры:
* GET: Безопасный и идемпотентный, так как запрос просто получает данные без изменения состояния.
* PUT: Не безопасный (он изменяет или создаёт ресурс), но идемпотентный (повторные запросы не изменят состояние после первого).
### Контекст использования:
* Безопасность важна, когда нужно быть уверенным, что запрос не повлияет на данные (например, при запросе информации).
* Идемпотентность важна для предотвращения непреднамеренных изменений при повторных запросах (например, обновление ресурса).

## 25.	Что такое endpoint?
Endpoint — это конкретный URL-адрес, по которому клиенты могут обращаться к ресурсу или сервису через API. Он определяет точку доступа для взаимодействия с сервером, отправки запросов и получения ответов.
📌Пример:
Если у вас есть API для управления пользователями, один из эндпоинтов может быть: https://api.example.com/users. Запросы к этому URL позволят получать список пользователей или добавлять новых.
Важность эндпоинтов в API заключается в том, что они упрощают доступ к различным ресурсам и операциям, делая взаимодействие с сервисами прозрачным и структурированным.

## 26.	Что такое статус код сервера?
Статус-код сервера — это числовой код в ответе от веб-сервера на запрос клиента (обычно веб-браузера), который указывает на результат обработки запроса. Эти коды помогают определить, была ли операция успешной, возникли ли ошибки и какие именно ошибки произошли.

## 27.	Какие существуют группы?
* 1xx: Информационные — запрос принят, продолжается обработка.
* 2xx: Успешные — запрос успешно обработан.
* 3xx: Перенаправление — клиенту нужно предпринять дополнительные действия для завершения запроса.
* 4xx: Ошибки клиента — ошибка на стороне клиента.
* 5xx: Ошибки сервера — ошибка на стороне сервера.

## 28.	Какие ты знаешь 5 из 400?
* Код статуса 400

 – Bad Request Запрос неправильный. Ошибка возникает в случае, если браузер клиента отправляет некорректный запрос серверу. Это может быть синтаксическая ошибка. Например, в запросе отсутствовали символы завершения строки.
* Код статуса 401

 – Unauthorized Этот код ошибки HTTP клиента сообщает, что на ресурс можно войти, используя действительный ID пользователя и пароль. Отказ в доступе также возникает, если пользователь неправильно ввел данные для авторизации (логин и пароль).
* Код статус 402

 – Payment Required Это нестандартный HTTP-статус. Он означает, что запрос не может быть выполнен, пока пользователь не произведет оплату. Код используется в платных пользовательских сервисах, а не в хостинговых провайдерах.
* Статус-код 403

 – Forbidden Запрет доступа к запрашиваемой странице. Он связан с тем, что у пользователя нет прав. Доступ может быть ограничен для определенных IP или в случае, если неавторизованный клиент пытается открыть файлы в системной папке. Этот код встречается, если сервер обнаружил вредоносные данные.
* Статус-код 404

 —  Not Found Это один из самых распространенных кодов ошибки HTTP клиента. Сервер дает ответ, что страница не найдена по данному URL. Например, страница перенесена на другой адрес. Не стоит путать код 404 с ошибкой «Сервер не найден». В данном случае клиент в состоянии общаться с сервером, но данных по его запросу нет.
* Статус-код 405

 —  Method Not Allowed Сервер сообщает, что используемый метод не может применяться на данном ресурсе. Он предложит доступные методы в заголовке Allow.
 409

 — Conflict Запрос пользователя вызывает конфликт с текущим состоянием сервера или несовместим с другим запросом.
* Код статуса 410

 – Gone Это ответ сервера в случае, если запрашиваемый контент больше недоступен или удален.

*  Статус-код 422

- Unprocessable Entity — сервер успешно принял запрос, может работать с указанным видом данных (например, в теле запроса находится XML-документ, имеющий верный синтаксис), однако имеется какая-то логическая ошибка, из-за которой невозможно произвести операцию над ресурсом. Введено в WebDAV.

## 29.	Какие ты знаешь 5 из 500?
* Статус код 500

– Internal Server Error
Код ошибки HTTP сервера появляется, когда он встречает ситуацию, обработка которой ему не знакома. Также возможен вариант, что данный запрос не поддерживается сервером и его обработка невозможна. В таких ситуациях сервер тоже отправляет HTTP-статус 500.

* Статус код 501

 — Not Implemented Ошибка 501 сообщает, что метод запроса сервером не поддерживается и его невозможно корректно обработать.
В ряде случаев в теле ошибки может быть указано: «Отправьте запрос позднее. Возможно, необходимая функция будет доступна».

* Статус код 503

 — Service Unavailable Сервер временно не доступен. Он не может обработать запрос клиента по следующим причинам:

* Статус код 505

 — HTTP Version Not Supported Этот код аналогичен 426. Он сообщает, что сервер не поддерживает версию протокола HTTP, который используется клиентом. Это случается, если используется устаревший формат HTTP-протокола. Решением проблемы будет установка одной версии.

## 30.	Какие ты знаешь Headers в Request?
Заголовки (Headers) в HTTP-запросах передают метаинформацию о запросе или ответе. Вот некоторые из основных заголовков:
* Accept: Указывает, какие форматы данных клиент может принять.
* 📌Пример: Accept: application/json
* Content-Type: Указывает тип содержимого, передаваемого в теле запроса.
* 📌Пример: Content-Type: application/json
* Authorization: Используется для аутентификации клиента.
* 📌Пример: Authorization: Bearer <token>
* User-Agent: Информация о клиентском приложении, отправляющем запрос.
* 📌Пример: User-Agent: Mozilla/5.0
* Host: Указывает доменное имя сервера.
* 📌Пример: Host: example.com
* Referer: Указывает URL-адрес, с которого пришёл запрос.
* 📌Пример: Referer: https://google.com
* Cookie: Отправляет куки, хранящиеся у клиента, на сервер.
* 📌Пример: Cookie: sessionId=abc123
* Cache-Control: Управляет кэшированием запроса/ответа.
* 📌Пример: Cache-Control: no-cache
* Accept-Encoding: Указывает, какие методы сжатия клиент может обработать.
* 📌Пример: Accept-Encoding: gzip, deflate
* Connection: Управляет параметрами соединения.
* 📌Пример: Connection: keep-alive
  
## 31.	Какие ты знаешь Headers в Responce?
Заголовки (Headers) в HTTP-ответе передают информацию о сервере и ответе на запрос клиента. Вот некоторые из ключевых заголовков:
* Content-Type: Указывает тип данных, возвращаемых сервером.
* 📌Пример: Content-Type: application/json
* Content-Length: Указывает размер тела ответа в байтах.
* 📌Пример: Content-Length: 348
* Date: Дата и время, когда был отправлен ответ.
* 📌Пример: Date: Wed, 21 Oct 2023 07:28:00 GMT
* Server: Информация о сервере, который обработал запрос.
* 📌Пример: Server: Apache/2.4.1 (Unix)
* Set-Cookie: Устанавливает куки на стороне клиента.
* 📌Пример: Set-Cookie: userId=abc123; Path=/;
* Cache-Control: Управляет кэшированием ответа.
* 📌Пример: Cache-Control: no-cache
* ETag: Уникальный идентификатор версии ресурса, используется для кэширования.
* 📌Пример: ETag: "686897696a7c876b7e"
* Location: Указывает URL для перенаправления клиента.
* 📌Пример: Location: https://example.com/new-resource
* Content-Encoding: Указывает метод сжатия данных.
* 📌Пример: Content-Encoding: gzip
* Allow: Указывает методы HTTP, которые поддерживаются для конкретного ресурса.
* 📌Пример: Allow: GET, POST

## 32.	Из чего состоит запрос HTTP - реквест?
HTTP-запрос состоит из нескольких ключевых компонентов:
### Стартовая строка:
* Метод: Тип запроса (GET, POST, PUT, DELETE и т.д.).
* URI: Унифицированный идентификатор ресурса (URL).
* Версия протокола: Версия HTTP (например, HTTP/1.1).

        📌Пример стартовой строки:
        GET /index.html HTTP/1.1
### Заголовки (Headers):
* Передают метаинформацию о запросе, клиентах и содержимом.
* Примеры заголовков: Host, Content-Type, User-Agent, Accept.

      📌Пример заголовков:
      Host: example.com
      Content-Type: application/json
      User-Agent: Mozilla/5.0
### Тело запроса (Body) (не всегда присутствует):
* Содержит данные, которые отправляются на сервер (например, JSON, XML, файлы).
* Обычно используется в методах POST и PUT.

      📌Пример тела запроса:
      {
        "name": "John Doe",
        "email": "john.doe@example.com"
      }

### Строка запроса (Query Parameters) (не всегда присутствует):
* Дополнительные параметры, передаваемые в URL.

      📌Пример: ?search=query&sort=desc

## 33.	Из чего состоят ответы HTTP -респонс?
HTTP-ответ (респонс) состоит из нескольких ключевых компонентов:
### Стартовая строка (Status Line):
* Версия протокола: Версия HTTP (например, HTTP/1.1).
* Код состояния (Status Code): Числовой код, указывающий на результат обработки запроса (например, 200, 404).
* Сообщение состояния (Reason Phrase): Краткое описание кода состояния (например, OK, Not Found).

      📌Пример стартовой строки:
      HTTP/1.1 200 OK

### Заголовки (Headers):
* Метаинформация о сервере и ответе.
* Примеры заголовков: Content-Type, Content-Length, Server.

      📌Пример заголовков:
      Content-Type: application/json
      Content-Length: 348
      Server: Apache/2.4.1 (Unix)

Тело ответа (Body) (не всегда присутствует):
* Содержит данные, возвращаемые сервером (например, HTML, JSON, изображения).
* Обычно используется для передачи основного контента.

      📌Пример тела ответа:
      {
        "message": "Hello, World!"
      }

Эти компоненты составляют полный HTTP-ответ, позволяя клиентам получать необходимые данные и информацию о состоянии обработки запроса. Всё стало ясно?

## 34.	Что такое и какая разница, Идентификация, Аутентификация, Авторизация?
### Идентификация
Идентификация — это процесс, при котором система определяет, кто именно пытается получить доступ к ее ресурсам. Пользователь сообщает системе свою идентификационную информацию, например, логин или идентификационный номер.

📌Пример: Когда ты вводишь логин или номер телефона на сайте, ты идентифицируешь себя системе.

### Аутентификация
Аутентификация — это процесс проверки подлинности предоставленных учетных данных, чтобы подтвердить, что пользователь действительно тот, за кого себя выдает. Обычно это достигается с помощью пароля, но может также включать биометрические данные, двухфакторную аутентификацию и т.д.

📌Пример: Когда ты вводишь пароль после ввода логина, система аутентифицирует тебя, проверяя, что пароль правильный.

### Авторизация
Авторизация — это процесс определения прав и уровней доступа пользователя к ресурсам после успешной аутентификации. Авторизация определяет, какие действия пользователь может выполнять и к каким данным иметь доступ.

📌Пример: После успешной аутентификации система проверяет, какие страницы и функции доступны тебе на основании твоих прав доступа, например, администратор может изменять настройки, а обычный пользователь — только просматривать данные.

### Схема процесса:
* Идентификация: Ввод логина -> система узнаёт, кто ты.
* Аутентификация: Ввод пароля -> система подтверждает, что это действительно ты.
* Авторизация: Проверка прав доступа -> система определяет, что тебе разрешено делать.

## 35.	Что происходит при валидации на сервере?
Валидация на сервере — это процесс проверки данных, отправленных с клиента, чтобы убедиться, что они соответствуют ожидаемым параметрам, и предотвратить возможные ошибки или атаки. Вот как это обычно работает:
* Получение данных: Сервер получает данные от клиента (например, через HTTP-запрос).
* Проверка структуры данных: Сервер проверяет, что данные соответствуют ожидаемой структуре. Например, если ожидается JSON-объект, сервер проверяет, что данные являются корректным JSON.
* Проверка типов данных: Сервер проверяет, что значения данных соответствуют ожидаемым типам (например, число, строка, дата).
* Проверка обязательных полей: Убедиться, что все необходимые поля присутствуют и не пусты.
* Проверка форматов: Проверка, что данные соответствуют определённым форматам (например, адрес электронной почты, номер телефона).
* Проверка логики и бизнес-правил: Сервер проверяет данные на соответствие бизнес-правилам и логике приложения (например, возраст пользователя должен быть больше 18 лет).
* Обработка ошибок: Если данные не проходят валидацию, сервер возвращает сообщение об ошибке клиенту с указанием, какие именно данные неверны.
* Сохранение данных: Если данные проходят все проверки, сервер сохраняет их в базе данных или выполняет другие операции.
* Ответ клиенту: Сервер отправляет ответ клиенту, подтверждающий успешную обработку данных или информирующий об ошибках.

## 36.	Что происходит при валидации на клиенте?
Процесс валидации на клиенте — это проверка данных прямо в браузере или приложении пользователя перед отправкой на сервер. Это помогает выявить ошибки и дать пользователю возможность исправить их на месте, снижая нагрузку на сервер и улучшая пользовательский опыт.
### Основные шаги валидации на клиенте:
* Получение данных: Пользователь вводит данные в формы или другие элементы ввода.
* Проверка структуры данных: Проверка, что данные соответствуют ожидаемому формату и структуре.
* Проверка типов данных: Проверка, что данные имеют правильные типы (например, строка, число).
* Проверка обязательных полей: Убедиться, что все обязательные поля заполнены.
* Проверка форматов: Проверка, что данные соответствуют определённым форматам.
* Проверка длины: Проверка, что данные соответствуют допустимой длине.
* Визуальные уведомления: Отображение ошибок и уведомлений пользователю в реальном времени.
* Отправка данных: Если данные проходят валидацию, они отправляются на сервер.

## 37.	Что такое токен?
Токен — это уникальный цифровой идентификатор, который используется для аутентификации и авторизации пользователей, а также для защиты данных и управления доступом в различных системах. Токены могут использоваться в различных контекстах, таких как веб-приложения, криптовалюты и API.
### Примеры токенов:
* Токен доступа (Access Token): Используется для аутентификации и авторизации пользователей в веб-приложениях и API.
* JWT (JSON Web Token): Это токен, закодированный в формате JSON, который используется для передачи данных между клиентом и сервером.
* Токены в криптовалютах: Представляют цифровые активы, которые можно передавать между пользователями.
* Токен безопасности (Security Token): Используется для двухфакторной аутентификации, обеспечивая дополнительный уровень защиты.

## 38.	Где хранится токен?
1. На клиенте:
### Local Storage:
* Хранит токены в хранилище браузера.
* Пример использования: localStorage.setItem('token', accessToken);
* Плюсы: Простота использования.
* Минусы: Уязвим для XSS-атак (межсайтовые скрипты).
### Session Storage:
* Похож на Local Storage, но данные сохраняются только на время сессии и удаляются после закрытия браузера.
* Пример использования: sessionStorage.setItem('token', accessToken);
* Плюсы: Более безопасно, чем Local Storage.
* Минусы: Уязвим для XSS-атак.
### Cookies:
* Хранение токенов в куках с параметром HttpOnly и Secure.
* Пример использования: Set-Cookie: token=accessToken; HttpOnly; Secure;
* Плюсы: Защита от XSS-атак, если используется HttpOnly.
* Минусы: Уязвим для CSRF-атак (подделка межсайтовых запросов).
2. На сервере:
### Серверная сессия:
* Токены хранятся на сервере, а клиенту выдается идентификатор сессии.
* Пример использования: Создание записи сессии в базе данных и передача идентификатора клиенту.
* Плюсы: Высокий уровень безопасности.
* Минусы: Требует дополнительных ресурсов на сервере для хранения сессий.
3. Рекомендации по безопасности:
* Использовать HTTPS для передачи токенов.
* Минимизировать время жизни токенов (TTL) и часто их обновлять.
* Использовать библиотеки и фреймворки, которые помогают безопасно работать с токенами.
* Регулярно проверять и обновлять методы аутентификации.
* 
## 39.	Что такое токен авторизации? Объясни процесс токен авторизации?
Токен авторизации — это цифровой ключ, который используется для подтверждения прав доступа пользователя к ресурсам или сервисам. Этот токен позволяет серверу определить, что пользователь имеет разрешение на выполнение определённых действий или доступ к определённой информации.
### Пример использования:
* При входе в веб-приложение, сервер может сгенерировать токен авторизации после успешной аутентификации пользователя (например, после ввода правильных логина и пароля).
* Токен передается клиенту и хранится на стороне клиента (в Local Storage, Session Storage или куках).
* При каждом запросе к защищённому ресурсу (например, для получения личных данных) клиент отправляет этот токен в заголовке запроса (обычно через заголовок Authorization).

Токены авторизации помогают безопасно управлять доступом к ресурсам и обеспечивают более гибкое и безопасное управление сессиями по сравнению с традиционными методами на основе cookies.

## 40.	Может ли быть авторизация без аутентификации? Приведи пример.
Авторизация без аутентификации — это редкий и непривычный сценарий, поскольку авторизация предполагает наличие проверки подлинности (аутентификации). В обычной практике аутентификация — это первый шаг, который проверяет, что вы есть тот, за кого себя выдаёте, а затем авторизация определяет, какие действия вам разрешены.
### Однако в определённых случаях можно представить сценарий, где авторизация без предварительной аутентификации возможна:
* Общественные данные: Доступ к общедоступным ресурсам, не требующим идентификации пользователя. Например, чтение новостных статей на сайте.
* API ключи: Некоторые API предоставляют доступ на основе ключей доступа, без необходимости аутентификации пользователя. Это своего рода упрощенная авторизация.
Общественные API с API-ключом: Некоторые API предоставляют доступ к своим ресурсам на основе предоставления ключа доступа, известного как API-ключ. Этот ключ является способом авторизации, но не требует аутентификации пользователя.
### 📌Пример:

Представь, что у тебя есть API, который предоставляет данные о погоде. Чтобы использовать этот API, разработчик должен получить ключ доступа.

Этот ключ предоставляет определенные права доступа к API, но не требует аутентификации каждого пользователя, кто использует этот ключ.

    GET /weather?location=Prague
    Authorization: Api-Key xxxxxxxxxx
В этом примере ключ (API-ключ) используется для авторизации доступа к погодным данным, но сам по себе он не аутентифицирует пользователя. Такой подход часто применяется для предоставления публичных данных или для ограниченного доступа без необходимости проверки подлинности.

## 41.	Что такое куки?
— это небольшие файлы, которые веб-сайты сохраняют на вашем устройстве (компьютере, смартфоне, планшете) для хранения информации о ваших действиях и предпочтениях. Эти файлы помогают сайтам запоминать вас и улучшать пользовательский опыт.
### Основные характеристики:
* Содержат данные: Куки могут хранить различные типы данных, такие как идентификаторы сеансов, предпочтения пользователя, информацию о корзине покупок и данные аутентификации.
* Создаются и читаются: Куки создаются веб-сервером и могут быть считаны как сервером, так и клиентом (браузером).
* Имеют срок действия: У куки есть срок действия, после которого они автоматически удаляются. Существуют сессионные куки (удаляются после закрытия браузера) и постоянные куки (хранятся на устройстве до истечения срока действия).
### Типы куки:
#### Сессионные куки (Session Cookies):
* Хранятся временно и удаляются после закрытия браузера.
* Используются для отслеживания состояния сеанса (например, корзина покупок).
#### Постоянные куки (Persistent Cookies):
* Хранятся на устройстве в течение заданного периода времени.
* Используются для хранения предпочтений пользователя и данных входа.
#### Куки сторонних разработчиков (Third-party Cookies):
* Создаются доменами, отличными от посещаемого сайта.
* Часто используются для рекламных целей и отслеживания активности пользователя на различных сайтах.
### Примеры использования:
* Аутентификация: Сохранение данных о входе, чтобы не нужно было повторно вводить логин и пароль при каждом посещении сайта.
* Настройки: Запоминание пользовательских настроек, таких как язык интерфейса или цветовая схема сайта.
* Аналитика: Отслеживание поведения пользователей для улучшения контента и пользовательского опыта.
* Реклама: Персонализация рекламных объявлений на основе предпочтений и поведения пользователя.
### Безопасность и конфиденциальность:
* Куки могут содержать конфиденциальные данные, поэтому их безопасность важна.
* HttpOnly: Флаг, предотвращающий доступ к куки из скриптов на стороне клиента, защищает от XSS-атак.
* Secure: Флаг, позволяющий передавать куки только по защищённому соединению (HTTPS).
### Управление куки:
* Пользователи могут управлять куки через настройки браузера, удалять их, блокировать или разрешать определённым сайтам устанавливать куки.

## 42.	Что такое теплый клиент?
Теплый клиент (Warm Client) — это клиент, который уже знаком с вашим брендом или продуктом, но пока не совершил покупку или не взял участие в вашем предложении. Этот тип клиентов находится на стадии привлечения и требует дополнительных усилий для превращения их в горячих (Hot) клиентов, которые готовы совершить покупку.
### Основные характеристики теплого клиента:
* Знакомство с брендом: Теплые клиенты уже знакомы с вашим брендом, но ещё не приняли решение о покупке.
* Интерес к продукту: Они проявляют интерес к вашему продукту или услуге, но могут нуждаться в дополнительной информации или убеждении.
* Потенциальные покупатели: Теплые клиенты имеют потенциал стать горячими клиентами, если предоставить им правильные стимулы и поддержку.
### Примеры взаимодействия с теплыми клиентами:
* Персонализированные предложения: Отправка персонализированных предложений и скидок, чтобы привлечь их внимание.
* Отзывы и рекомендации: Предоставление отзывов и рекомендаций от других клиентов, чтобы укрепить доверие.
* Контент: Регулярная отправка полезного контента, который помогает клиентам лучше понять ваш продукт и его преимущества.
### Важность работы с теплыми клиентами:
* Работа с теплыми клиентами важна, так как они уже проявили интерес к вашему бренду и могут стать лояльными покупателями с дополнительными усилиями. Эффективное управление теплыми клиентами может значительно увеличить продажи и улучшить отношения с клиентами.

## 43.	Что такое холодный клиент?
Холодный клиент — это потенциальный клиент, который ещё не проявил интереса к вашему продукту или услуге. Обычно это люди, которые не знакомы с вашим брендом и не взаимодействовали с вами ранее. Работа с холодными клиентами требует больше усилий, чтобы привлечь их внимание и пробудить интерес.
### Основные характеристики холодного клиента:
* Отсутствие знакомства: Эти клиенты не знают о вашем бренде или продуктах.
* Низкий уровень интереса: Они ещё не проявили интереса к вашим предложениям.
* Не готов к покупке: Холодные клиенты не рассматривают покупку вашего продукта на данном этапе.
### Стратегии работы с холодными клиентами:
* Повышение узнаваемости бренда: Использование рекламы, социальных сетей и контент-маркетинга, чтобы познакомить клиентов с вашим брендом.
* Информативный контент: Предоставление полезной и ценной информации, которая может быть интересна холодным клиентам.
* Привлекательные предложения: Промо-акции и специальные предложения, которые могут привлечь внимание и вызвать интерес.
* Лидогенерация: Использование методов лидогенерации, таких как вебинары, бесплатные пробные версии или демо-продукты, чтобы заинтересовать потенциальных клиентов и вовлечь их в ваше предложение.
### Примеры взаимодействия с холодными клиентами:
* Реклама: Размещение объявлений в социальных сетях и на платформах, где обитает ваша целевая аудитория.
* Контент-маркетинг: Создание блогов, статей и видео, которые решают проблемы и отвечают на вопросы вашей целевой аудитории.
* Email-маркетинг: Отправка информативных и полезных писем с целью заинтересовать клиента и подвести его к следующему шагу.
### Важность работы с холодными клиентами:
* Работа с холодными клиентами важна, потому что они представляют собой потенциальный рост для вашего бизнеса. Привлечение и конвертация холодных клиентов в тёплых и горячих клиентов может значительно расширить вашу клиентскую базу и увеличить продажи.

## 44.	Что такое кеш?
Кэш — это специальный буфер или область памяти, предназначенная для временного хранения данных, которые часто или недавно использовались. Это помогает ускорить доступ к данным и уменьшить нагрузку на основную систему или источник данных.
### Основные характеристики кэша:
* Быстродействие: Кэш предназначен для быстрого доступа к данным по сравнению с основным хранилищем.
* Временное хранение: Данные хранятся в кэше временно и могут быть обновлены или удалены по мере необходимости.
* Частота использования: В кэше обычно хранятся данные, к которым часто обращаются или которые недавно использовались.
### Типы кэша:
#### Кэш процессора:
* Находится внутри процессора и служит для хранения данных и инструкций, к которым процессор обращается чаще всего.
* Уровни кэша (L1, L2, L3) различаются по размеру и скорости доступа.
#### Кэш браузера:
* Хранит копии веб-страниц, изображений и других ресурсов, чтобы ускорить загрузку при повторных посещениях сайтов.
* Пример: Когда вы возвращаетесь на часто посещаемый веб-сайт, браузер использует кэш для быстрой загрузки страниц.
#### Кэш DNS:
* Хранит результаты разрешения DNS-запросов, чтобы ускорить доступ к часто посещаемым доменам.
* Пример: Когда вы посещаете сайт google.com, DNS-запись кэшируется для быстрого доступа в следующий раз.
#### Кэш баз данных:
* Хранит результаты запросов к базе данных, чтобы уменьшить нагрузку на сервер и ускорить доступ к данным.
* Пример: Часто запрашиваемые записи базы данных могут быть кэшированы для ускоренного доступа.
#### Кэш веб-сервера:
* Хранит динамически создаваемые страницы и другие ресурсы для быстрого доступа пользователям.
* Пример: Кэширование страниц на сервере WordPress для ускорения их загрузки.
### Виды кэширования:
* Кэширование по требованию: Данные кэшируются, когда к ним впервые обращаются.
* Предварительное кэширование: Данные кэшируются заранее, до обращения к ним.
* Адаптивное кэширование: Кэширование данных на основе анализа их использования.
### Преимущества кэша:
* Повышение производительности: Быстрый доступ к кэшированным данным ускоряет работу системы.
* Снижение нагрузки: Уменьшение количества обращений к основному источнику данных снижает нагрузку на серверы и сеть.
* Экономия ресурсов: Сокращение времени обработки запросов экономит вычислительные ресурсы.
### Недостатки кэша:
* Ограниченный объём памяти: Кэш имеет ограниченное пространство, и старые данные могут быть вытеснены новыми.
* Актуальность данных: Иногда данные в кэше могут быть устаревшими, если основная копия данных изменилась.
* Сложность управления: Эффективное управление кэшом требует дополнительной настройки и мониторинга.
* Кэширование — это мощный инструмент для повышения производительности и эффективности систем, но он требует тщательного управления для обеспечения актуальности и оптимальности данных.

## 45.	Для чего используется кэширование страниц?
Кэширование страниц — это процесс сохранения копий веб-страниц для уменьшения времени загрузки и снижения нагрузки на сервер. Это значительно повышает производительность и улучшает пользовательский опыт.
### Принципы кэширования страниц:
* Сохранение копий страниц: Веб-страницы сохраняются в кэше (локально на устройстве пользователя или на сервере), чтобы при повторном посещении они загружались быстрее.
* Время жизни (TTL): Устанавливается время, на которое страница сохраняется в кэше. После его истечения страница обновляется.
### Типы кэша:
* Браузерный кэш: Хранит страницы и ресурсы локально на устройстве пользователя.
* Серверный кэш: Хранит страницы на сервере или CDN (Content Delivery Network).
### 📌Примеры кэширования страниц:
* WordPress: Популярные плагины для кэширования, такие как W3 Total Cache или WP Super Cache, создают статические HTML-копии страниц для их быстрой загрузки.
* CDN: Сервисы, такие как Cloudflare или Akamai, кэшируют страницы и ресурсы на серверах по всему миру, обеспечивая быструю загрузку для пользователей из разных регионов.
### Преимущества:
* Уменьшение времени загрузки: Быстрая загрузка страниц улучшает пользовательский опыт.
* Снижение нагрузки на сервер: Кэширование уменьшает количество запросов к основному серверу.
* Экономия ресурсов: Меньшее количество вычислительных ресурсов требуется для обработки повторных запросов.
### Недостатки:
* Устаревание данных: Кэшированные страницы могут содержать устаревшую информацию.
* Усложнение управления: Настройка и управление кэшированием требует дополнительных усилий.
### Использование заголовков:
* Cache-Control: Управляет поведением кэширования.

        📌Пример: Cache-Control: max-age=3600 указывает, что страница может кэшироваться на 1 час.
        Expires: Устанавливает дату, после которой страница считается устаревшей.
        📌Пример: Expires: Wed, 21 Oct 2023 07:28:00 GMT
        ETag: Используется для проверки актуальности кэшированного ресурса.
        📌Пример: ETag: "686897696a7c876b7e"
## 46.	Для чего необходимо сбрасывать кэш и куки?
### Сбрасывание кэша и куки необходимо для нескольких важных целей:
#### Для чего необходимо сбрасывать кэш:
* Свежие данные: Кэш может хранить устаревшую информацию. Сброс кэша гарантирует, что вы получаете самые последние версии страниц и ресурсов.
* Исправление ошибок: Иногда кэшированные данные могут вызвать ошибки в отображении или функциональности сайта. Сброс кэша помогает устранить такие проблемы.
* Очистка пространства: Кэш может занимать значительное количество дискового пространства. Регулярное очищение кэша освобождает место на вашем устройстве.
#### Для чего необходимо сбрасывать куки:
* Обновление авторизации: Куки могут содержать данные о сеансах и аутентификации. Сброс куки может помочь устранить проблемы с входом или выходом из аккаунта.
* Устранение проблем с конфиденциальностью: Куки могут отслеживать вашу активность в интернете. Очистка куки помогает защитить вашу приватность.
* Решение проблем с производительностью: Накопление большого количества куки может замедлить работу браузера. Очистка куки может улучшить производительность.

## 47.	Что такое сессия?
Сессия — это временное взаимодействие между пользователем и системой, которое длится в течение определённого периода времени. Она используется для сохранения состояния и данных пользователя на сервере или в приложении, чтобы обеспечить непрерывный и персонализированный опыт.
### Основные характеристики сессии:
* Временность: Сессия существует только на время взаимодействия пользователя с системой. После завершения сессии данные могут быть удалены.
* Уникальный идентификатор: Каждая сессия имеет уникальный идентификатор (session ID), который помогает системе распознать пользователя и его действия.
* Хранение данных: Сессия может хранить данные пользователя, такие как идентификатор пользователя, данные корзины покупок, настройки и т.д.
### Примеры использования сессий:
* Интернет-магазины: Данные о товарах в корзине сохраняются в течение сессии, чтобы пользователь мог продолжить покупки при переходе между страницами.
* Авторизация: После входа в систему, данные о пользователе сохраняются в сессии, чтобы не нужно было вводить логин и пароль на каждой странице.
* Пользовательские настройки: Сохранение предпочтений пользователя, таких как язык интерфейса или тема оформления, на время сеанса.
### Управление сессиями:
* Создание сессии: При первом взаимодействии пользователя с системой создаётся новая сессия и присваивается уникальный идентификатор.
* Хранение сессии: Данные сессии могут храниться на сервере или на стороне клиента (например, в куки).
* Истечение сессии: Сессия может быть завершена по истечении определенного времени или при выходе пользователя из системы.

## 48.	Какие стореджи браузера ты знаешь?
### Cookies:
* Используются для хранения данных сеансов и аутентификации.
* Данные сохраняются на стороне клиента и передаются на сервер с каждым запросом.
* 📌Примеры: данные входа, предпочтения пользователя.
### Local Storage:
* Позволяет хранить данные в браузере на длительное время.
* Данные сохраняются до тех пор, пока их не удалит сам сайт или пользователь.
* 📌Примеры: настройки сайта, пользовательские предпочтения.
### Session Storage:
* Хранит данные только на время текущей сессии.
* Данные удаляются после закрытия вкладки или окна браузера.
* 📌Примеры: временные данные формы, текущий статус пользователя.
### IndexedDB:
* База данных на стороне клиента для хранения больших объёмов структурированных данных.
* Позволяет осуществлять сложные запросы и транзакции.
* 📌Примеры: данные приложений, оффлайн-хранение данных.
### Web SQL Database (устаревший):
* База данных SQL на стороне клиента.
* Позволяет сохранять и манипулировать данными с использованием SQL-запросов.
* 📌Примеры: схожи с IndexedDB, но используется реже.
### Cache Storage:
* Хранит ресурсы, такие как HTML, CSS и JavaScript файлы, для использования оффлайн.
* Используется в Service Workers для создания прогрессивных веб-приложений (PWA).
* 📌Примеры: кэшированные страницы и ресурсы для оффлайн-доступа.

## 49.	В чем разница между сешин сторедж и локал сторедж?
Session Storage и Local Storage — это два типа хранилищ в браузере, которые помогают сохранять данные на стороне клиента. Вот их основные различия:
### Session Storage:
* Временность: Данные хранятся только на время текущей сессии браузера. Они удаляются после закрытия вкладки или окна.
* Объем данных: Обычно ограничен до 5 МБ на домен.
* Доступность: Данные доступны только в пределах вкладки или окна, в которых были созданы. Они не могут быть доступны в других вкладках или окнах того же браузера.
* Использование: Подходит для хранения временных данных, таких как данные формы или текущий статус пользователя.

        📌Пример использования Session Storage:
        sessionStorage.setItem('key', 'value');
        let data = sessionStorage.getItem('key');

### Local Storage:
* Постоянность: Данные сохраняются до тех пор, пока их не удалит сам сайт или пользователь. Данные остаются даже после закрытия и повторного открытия браузера.
* Объем данных: Обычно ограничен до 5-10 МБ на домен.
* Доступность: Данные доступны во всех вкладках и окнах браузера, принадлежащих одному домену.
* Использование: Подходит для хранения постоянных данных, таких как настройки пользователя, темы или другие предпочтения.

        📌Пример использования Local Storage:
        localStorage.setItem('key', 'value');
        let data = localStorage.getItem('key');

### Краткое сравнение:
* Длительность хранения: Session Storage — временное, Local Storage — постоянное.
* Доступность: Session Storage — только в пределах текущей сессии, Local Storage — во всех сессиях.

## 50.	Для чего необходимо сбрасывать кэш и куки?
### Сбрасывание кэша и куки необходимо для нескольких важных целей:
#### Для чего необходимо сбрасывать кэш:
* Свежие данные: Кэш может хранить устаревшую информацию. Сброс кэша гарантирует, что вы получаете самые последние версии страниц и ресурсов.
* Исправление ошибок: Иногда кэшированные данные могут вызвать ошибки в отображении или функциональности сайта. Сброс кэша помогает устранить такие проблемы.
* Очистка пространства: Кэш может занимать значительное количество дискового пространства. Регулярное очищение кэша освобождает место на вашем устройстве.
#### Для чего необходимо сбрасывать куки:
* Обновление авторизации: Куки могут содержать данные о сеансах и аутентификации. Сброс куки может помочь устранить проблемы с входом или выходом из аккаунта.
* Устранение проблем с конфиденциальностью: Куки могут отслеживать вашу активность в интернете. Очистка куки помогает защитить вашу приватность.
* Решение проблем с производительностью: Накопление большого количества куки может замедлить работу браузера. Очистка куки может улучшить производительность.

## 51.	Что такое REST
Передача репрезентативного состояния (REST) - это архитектурный стиль для создания веб-сервисов. Впервые он был представлен в 2000 году Роем Филдингом в его докторской диссертации, и с тех пор стал популярным выбором для разработки API (интерфейсов прикладного программирования) для веб-приложений. В основе REST лежат несколько простых принципов, которые делают его мощным инструментом для создания масштабируемых, надежных и простых в обслуживании веб-сервисов. Основная идея REST заключается в использовании протокола HTTP, который является основой Всемирной паутины, для определения стандартного способа обмена данными между клиентами и серверами.
### Основные принципы REST включают следующее:
* Клиент-серверная архитектура: Клиент и сервер должны быть отделены друг от друга, и каждый из них должен иметь возможность развиваться независимо. Такое разделение задач позволяет добиться большей масштабируемости, гибкости и простоты системы.
* Нестационарность: Каждый запрос содержит всю информацию, необходимую для его выполнения, поэтому серверу не нужно отслеживать данные сеанса. Это делает RESTful API очень масштабируемыми и удобными для кэширования, поскольку каждый запрос может обрабатываться независимо.
* Возможность кэширования: Ответы должны иметь возможность кэширования на клиенте или промежуточных серверах для повышения производительности.
* Многоуровневая система: REST позволяет использовать архитектуру многоуровневой системы, где каждый уровень независим и может взаимодействовать только со своим непосредственным уровнем. Такая архитектура способствует масштабируемости и позволяет добавлять или удалять слои по мере необходимости.
* Единообразный интерфейс: API REST должны иметь единый интерфейс, что означает, что каждый ресурс идентифицируется уникальным URL и что для взаимодействия с ресурсами используются стандартные методы HTTP.
### Методы HTTP
Протокол HTTP предоставляет набор методов, которые могут быть использованы для взаимодействия с ресурсами. Наиболее распространенными методами, используемыми в RESTful API, являются GET, POST, PUT, PATCH и DELETE. Вот краткий обзор каждого метода:
* GET: получение данных с сервера. Этот метод используется для получения ресурса с сервера.
* POST: создает новый ресурс на сервере. Этот метод используется для создания нового ресурса на сервере.
* PUT: обновление существующего ресурса на сервере. Этот метод используется для обновления существующего ресурса на сервере.
* PATCH: частичное обновление существующего ресурса на сервере. Этот метод используется для обновления части существующего ресурса на сервере.
* DELETE: удаляет ресурс с сервера. Этот метод используется для удаления ресурса с сервера.
### Идентификация ресурса
В архитектуре RESTful каждый ресурс идентифицируется уникальным URL, который называется конечной точкой ресурса. Например, запись в блоге может иметь URL-адрес https://example.com/posts/123, где "123" - это ID записи. Клиент может взаимодействовать с ресурсом, посылая HTTP-запросы к его конечной точке, а сервер отвечает HTTP-ответом, содержащим запрошенные данные.
### Формат ответа
Формат ответа для RESTful API обычно представляет собой JSON (JavaScript Object Notation) или XML (Extensible Markup Language). JSON - это легкий формат обмена данными, который легко читать и писать, и он поддерживается большинством современных языков программирования. XML - это язык разметки, который сложнее, чем JSON, но также широко используется в веб-разработке.
### Гипермедиа
Еще одним важным аспектом REST является использование гипермедиа. Гипермедиа - это практика включения в ответ ссылок на связанные ресурсы, что облегчает клиентам навигацию по API и открытие новых ресурсов. Это позволяет создать более гибкий и динамичный дизайн API, поскольку клиенты могут адаптироваться к изменениям в API без сбоев.
### Преимущества REST
Существует несколько преимуществ использования REST для создания веб-служб. К ним относятся следующие:
* Простота: REST - это простая архитектура, которую легко понять и реализовать. Это делает ее популярной среди разработчиков, поскольку она позволяет им быстро создавать масштабируемые и надежные веб-службы.
* Масштабируемость: RESTful API обладают высокой масштабируемостью, поскольку они не имеют статических данных и каждый запрос может обрабатываться независимо. Это позволяет повысить производительность и способность обрабатывать большое количество запросов.
* Гибкость: REST позволяет создать гибкий дизайн API, поскольку ресурсы можно добавлять, удалять или изменять, не затрагивая другие части API. Это обеспечивает большую адаптивность и возможность развития API с течением времени.
* Совместимость: REST совместим практически с любым языком программирования или платформой, поскольку использует стандартные методы HTTP и форматы ответов. Это обеспечивает большую совместимость и возможность интеграции с другими системами.
* Кэширование: REST позволяет кэшировать ответы на клиенте или промежуточных серверах, что может значительно повысить производительность и снизить нагрузку на сервер.
### Недостатки REST
Несмотря на то, что использование REST имеет множество преимуществ, необходимо учитывать и некоторые потенциальные недостатки. К ним относятся следующие:
* Накладные расходы: REST иногда может иметь больше накладных расходов, чем другие архитектуры, поскольку каждый запрос включает всю информацию, необходимую для его выполнения. Это может привести к увеличению полезной нагрузки и снижению производительности.
* Сложность: Хотя REST является простой архитектурой, проектирование RESTful API может быть сложным и трудоемким. Это требует тщательного рассмотрения идентификации ресурсов, форматов ответов и гипермедиа, среди прочего.
* Безопасность: RESTful API могут быть уязвимы для угроз безопасности, таких как инъекционные атаки или несанкционированный доступ. Для снижения этих рисков необходимо применять надлежащие меры безопасности, такие как аутентификация и шифрование.
### Заключение
В заключение следует отметить, что REST - это мощная архитектура для создания веб-служб, основанная на нескольких простых принципах. Благодаря использованию протокола HTTP, RESTful API позволяют создавать масштабируемые, надежные и простые в обслуживании веб-службы, совместимые практически с любым языком программирования или платформой. Несмотря на некоторые потенциальные недостатки, преимущества REST делают его популярным выбором для разработчиков, которым необходимо быстро и эффективно создавать веб-сервисы.

## 52.	Что такое SOAP
SOAP (Simple Object Access Protocol) — это протокол обмена сообщениями для передачи структурированных данных в распределенных вычислительных средах. Он использует XML для форматирования сообщений и основан на других интернет-протоколах, таких как HTTP и SMTP.
### Основные характеристики SOAP:
* Протокол на основе XML: SOAP- сообщения формируются и интерпретируются в формате XML, что обеспечивает гибкость и расширяемость.
* Транспортный уровень: SOAP может работать поверх различных транспортных протоколов, таких как HTTP, SMTP, TCP и др.
* Бессостояние: Каждое SOAP-сообщение является самодостаточным и не зависит от других сообщений.
* Стандартизированные спецификации: SOAP строго определен набором спецификаций, что обеспечивает совместимость между различными системами и платформами.
### Основные компоненты SOAP:
* Envelope: Оболочка, которая обрамляет сообщение и определяет, что оно является SOAP-сообщением.
* Header: Опциональный элемент, который содержит информацию для маршрутизации и управления сообщениями.
* Body: Основная часть сообщения, которая содержит фактические данные или вызовы методов.
* Fault: Опциональный элемент внутри Body, который используется для передачи информации об ошибках.

        📌 Пример SOAP-сообщения:
        <soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ex="http://example.com/">
           <soapenv:Header/>
           <soapenv:Body>
              <ex:ExampleRequest>
                 <ex:Message>Hello, SOAP!</ex:Message>
              </ex:ExampleRequest>
           </soapenv:Body>
        </soapenv:Envelope>

### Преимущества SOAP:
* Формальность и строгость: Обеспечивает высокую степень стандартизации и формальности.
* Расширяемость: Легко расширяется за счёт использования XML.
* Безопасность: Поддерживает различные стандарты безопасности, такие как WS-Security.
### Недостатки SOAP:
* Сложность: Требует больше усилий для реализации и управления по сравнению с REST.
* Производительность: Из-за использования XML и сложной структуры сообщений, SOAP может быть медленнее, чем более лёгкие протоколы.
* SOAP часто используется в корпоративных системах, где важны формальность, строгость и безопасность передачи данных.

## 53.	Чем REST отличается от SOAP?
1. Архитектурный стиль vs. Протокол
* REST: Это архитектурный стиль, который использует стандартные методы HTTP для взаимодействия с веб-сервисами. REST не имеет жестких стандартов или форматов, что делает его гибким и легким для использования.
* SOAP: Это протокол для обмена сообщениями, который имеет строгие стандарты для формата данных (XML) и поддерживает множество расширений (безопасность, транзакции и т.д.).
2. Формат данных
* REST: Поддерживает различные форматы данных, такие как JSON, XML, HTML, Plain Text и другие. Наиболее часто используется JSON из-за его легкости и простоты.
* SOAP: Всегда использует XML для представления данных. Это более сложный и тяжелый формат, который может содержать дополнительные метаданные и требует больше ресурсов для обработки.
3. Простота и сложность
* REST: Более легкий и простой в реализации. Он полагается на стандартные методы HTTP (GET, POST, PUT, DELETE и т.д.) и не требует сложных спецификаций для описания API. Это делает его идеальным для простых сервисов.
* SOAP: Более сложен в использовании, так как требует четкой спецификации операций через WSDL (Web Services Description Language). SOAP строго определяет формат сообщений и требует использования дополнительных механизмов (например, для обработки ошибок).
4. Гибкость и стандартизация
* REST: Более гибкий и не навязывает жестких стандартов. Он позволяет разработчику выбирать любой формат данных, кэширование, методы аутентификации и так далее. REST может адаптироваться к различным требованиям.
* SOAP: Строго регламентирован. Все операции и сообщения стандартизированы (через XML и WSDL). SOAP поддерживает множество стандартов (например, WS-Security, WS-ReliableMessaging), что делает его предпочтительным в сложных корпоративных средах.
5. Надежность и безопасность
* REST: Может использовать стандартные методы безопасности HTTP, такие как SSL/TLS (HTTPS) для шифрования. Однако REST не поддерживает встроенные стандарты для сложной безопасности и транзакционности.
* SOAP: Обладает встроенной поддержкой расширенных стандартов безопасности через WS-Security. Это позволяет реализовать шифрование, цифровые подписи и управление безопасностью на уровне сообщения. SOAP также поддерживает надежную доставку сообщений через WS-ReliableMessaging и транзакции (например, ACID).
6. Применение и использование
* REST: Идеален для взаимодействия с легкими веб-приложениями, мобильными приложениями, микросервисами и API, которые не требуют сложных транзакционных механизмов или повышенной безопасности. REST API легко кэшируются и масштабируются.
* SOAP: Предпочтителен для корпоративных приложений, требующих сложных транзакций, высокой степени безопасности и надежности (например, банковские системы, системы ERP, системы управления ресурсами). SOAP часто используется для интеграции бизнес-систем и взаимодействия в закрытых корпоративных сетях.
7. Кэширование
* REST: Позволяет легко кэшировать данные, так как использует стандартные HTTP-заголовки для управления кэшированием. Это повышает производительность, особенно при повторяющихся запросах.
* SOAP: Не поддерживает кэширование напрямую, так как запросы SOAP всегда направлены на выполнение операций, а не на чтение данных.
8. Стандарты для описания API
* REST: Обычно не требует обязательного описания API, хотя для документирования часто используют инструменты, такие как Swagger или OpenAPI. REST не зависит от таких форматов, что делает его более гибким.
* SOAP: Использует WSDL (Web Services Description Language) для точного описания интерфейса веб-сервиса. Это позволяет клиенту точно знать, какие операции поддерживаются и какие параметры передаются.
9. Производительность
* REST: Обычно быстрее, поскольку использует легкие форматы данных (например, JSON), и запросы более минималистичны по сравнению с SOAP. REST-сервисы также легче кэшируются, что может повысить производительность.
* SOAP: Медленнее из-за использования тяжелого формата XML, большого количества метаданных и дополнительных проверок безопасности, если они включены.

## 54.	JSON Schema - это?
JSON Schema — это спецификация для описания структуры и проверки данных в формате JSON. 
Схема JSON предоставляет способ определить правила для данных JSON, чтобы гарантировать, что они соответствуют определенным требованиям. Она похожа на механизм валидации и описания типов данных, которые могут быть использованы в веб-приложениях, API и других системах, работающих с JSON.
### Основные возможности JSON Schema:
* Определение структуры данных: JSON Schema позволяет точно описать, какие поля должны присутствовать в объекте, их типы (например, строки, числа, массивы, объекты), какие поля обязательны, какие — опциональны, и другие характеристики.
* Валидация данных: С помощью схемы можно проверить, соответствуют ли предоставленные данные заранее определенной структуре. Это помогает предотвратить ошибки, связанные с некорректными форматами данных.
* Документация: JSON Schema также может служить для описания API или форматов данных, позволяя разработчикам легко понять, что требуется от них при передаче данных в JSON-формате.
### Основные элементы JSON Schema:
* type: Указывает тип данных (например, объект, массив, строка, число).

        {
          "type": "object"
        }

* properties: Определяет свойства объекта и их типы.

        {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "age": {
              "type": "number"
            }
          }
        }

* required: Указывает обязательные свойства.

        {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "age": {
              "type": "number"
            }
          },
          "required": ["name", "age"]
        }

* items: Определяет схему для элементов массива.

        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }

* enum: Указывает возможные значения для свойства.

        {
          "type": "string",
          "enum": ["red", "green", "blue"]
        }

* 📌Пример JSON Schema:

        {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "title": "Пример схемы",
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            },
            "age": {
              "type": "integer",
              "minimum": 0
            },
            "email": {
              "type": "string",
              "format": "email"
            }
          },
          "required": ["name", "age"]
        }
JSON Schema широко используется для валидации данных в API и базах данных, а также для генерации документации и автоматического тестирования.

## 55.	Что такое JSON и XML?
JSON (JavaScript Object Notation) и XML (eXtensible Markup Language) — это два формата для хранения и передачи данных, часто используемые в веб-приложениях и API.
### JSON:
* Формат: Лёгкий, текстовый формат данных, удобный для чтения и записи человеком.
* Синтаксис: Основывается на JavaScript синтаксисе, но используется независимо от него.
* Использование: Широко применяется для обмена данными между клиентом и сервером в веб-приложениях.

        📌Пример:
        {
          "name": "John",
          "age": 30,
          "city": "New York"
        }
### XML:
* Формат: Более сложный и гибкий формат для описания, хранения и передачи данных.
* Синтаксис: Основывается на тегах, как в HTML.
* Использование: Применяется в различных областях, включая конфигурационные файлы, документацию и веб-сервисы.

        📌Пример:
        <person>
          <name>John</name>
          <age>30</age>
          <city>New York</city>
        </person>
### Сравнение:
* Простота: JSON проще в использовании и легче читается человеком.
* Гибкость: XML более гибкий и позволяет создавать более сложные структуры данных.
* Объем: JSON обычно занимает меньше места и быстрее передаётся по сети.
* Поддержка: JSON нативно поддерживается многими языками программирования, в то время как XML требует дополнительных библиотек.

Итак, JSON и XML предлагают разные подходы к работе с данными, и выбор между ними зависит от конкретных потребностей и требований проекта.

## 56.	Каким форматом данных могут быть ключи в JSON?
* Строки (string) — текстовые данные, заключенные в двойные кавычки.
* Числа (number) — целые и дробные числа.
* Логические значения (boolean) — два значения: true или false.
* Массивы (array) — упорядоченные списки значений, заключенные в квадратные скобки [].
* Объекты (object) — наборы пар "ключ-значение", заключенные в фигурные скобки {}.
* null — специальное значение, которое обозначает отсутствие данных.

## 57.	Каким форматом данных могут быть значения в JSON?
* Функции (Functions)
JSON не поддерживает функции или любой исполняемый код.
* Неопределенные значения (undefined)
Значение undefined, используемое в JavaScript для обозначения переменных без значения, недопустимо в JSON.
* Дата (Date)
JSON не имеет встроенного типа данных для представления дат. Обычно даты передаются в виде строк.
* Инфинити и NaN
Значения Infinity, -Infinity и NaN (Not a Number), которые допустимы в JavaScript, недопустимы в JSON.
* Комментарии
В JSON нельзя использовать комментарии. В отличие от некоторых языков разметки или программирования, JSON не поддерживает синтаксис комментариев (// или /* */).
* Специальные объекты (например, RegExp)
* Циклические ссылки
JSON не может содержать циклические ссылки, когда объект ссылается на себя или через другие объекты. Попытка сериализации таких объектов приведет к ошибке.

## 58.	Какие невалидные типы данных есть у JSON?
В JSON ключи и значения могут быть только следующих типов данных: строки, числа, объекты, массивы, булевы значения и null. Всё остальное будет считаться невалидным:
### Невалидные типы данных в JSON:
* Функции: JSON не поддерживает функции.

        📌Пример:
        {
          "calculate": function() { return 1 + 1; }
        }
* Дата и время: JSON не имеет встроенного типа для хранения даты и времени.

        📌Пример:
        {
          "date": new Date()
        }
* Неопределенные значения: JSON не поддерживает значение undefined.

        📌Пример:
        {
          "name": undefined
        }
* Другие объекты JavaScript: Такие как Map, Set, WeakMap, WeakSet и другие.

        📌Пример:
        {
          "items": new Set([1, 2, 3])
        }
* Для работы с датами и временем часто используется формат строки ISO 8601, а для функций и специальных объектов необходимо преобразование в поддерживаемые типы.

## 59.	Что такое WSDL
WSDL (Web Services Description Language) — это стандартный язык для описания веб-сервисов и способ их взаимодействия. WSDL используется в архитектуре SOAP для описания доступных методов веб-сервиса, параметров и структур данных.
### Основные компоненты WSDL:
* Types: 
Описание типов данных, которые используются в веб-сервисе. Обычно основано на XML Schema.
* Message:
Определение сообщений, которые обмениваются между клиентом и сервером. Сообщения состоят из одной или нескольких частей.
* PortType:
Описание операций (методов), доступных в веб-сервисе, и сообщений, связанных с каждой операцией.
* Binding:
Описание конкретного протокола и форматов данных, которые используются для каждой операции и сообщения. Чаще всего это SOAP.
* Service:
Указывает URL-адреса, по которым доступен веб-сервис.

        📌Пример WSDL:
        <definitions xmlns="http://schemas.xmlsoap.org/wsdl/"
                     xmlns:soap="http://schemas.xmlsoap.org/wsdl/soap/"
                     xmlns:tns="http://example.com/wsdl"
                     targetNamespace="http://example.com/wsdl">
          <types>
            <xsd:schema targetNamespace="http://example.com/wsdl">
              <xsd:element name="getGreetingRequest" type="xsd:string"/>
              <xsd:element name="getGreetingResponse" type="xsd:string"/>
            </xsd:schema>
          </types>
        
          <message name="getGreetingRequest">
            <part name="name" element="tns:getGreetingRequest"/>
          </message>
          <message name="getGreetingResponse">
            <part name="greeting" element="tns:getGreetingResponse"/>
          </message>
        
          <portType name="GreetingPortType">
            <operation name="getGreeting">
              <input message="tns:getGreetingRequest"/>
              <output message="tns:getGreetingResponse"/>
            </operation>
          </portType>
        
          <binding name="GreetingBinding" type="tns:GreetingPortType">
            <soap:binding style="rpc" transport="http://schemas.xmlsoap.org/soap/http"/>
            <operation name="getGreeting">
              <soap:operation soapAction="http://example.com/wsdl/getGreeting"/>
              <input>
                <soap:body namespace="http://example.com/wsdl" use="literal"/>
              </input>
              <output>
                <soap:body namespace="http://example.com/wsdl" use="literal"/>
              </output>
            </operation>
          </binding>
        
          <service name="GreetingService">
            <port name="GreetingPort" binding="tns:GreetingBinding">
              <soap:address location="http://example.com/wsdl"/>
            </port>
          </service>
        </definitions>
Этот пример показывает, как WSDL описывает веб-сервис, его методы и сообщения.

## 60.	  Что такое WADL
WADL (Web Application Description Language) — это язык описания веб-приложений, предназначенный для описания RESTful веб-сервисов, аналогично тому, как WSDL используется для SOAP веб-сервисов. WADL помогает разработчикам документировать и автоматизировать взаимодействие с RESTful сервисами.
### Основные элементы WADL:
* Application:
Корневой элемент, который описывает веб-приложение.
* Resources:
Описывает коллекции ресурсов, доступных в веб-приложении, и их URI.
* Resource:
Описывает конкретный ресурс, его методы и параметры.
* Method:
Описывает методы HTTP (GET, POST, PUT, DELETE) и их поведение.
* Request:
Описывает параметры запроса, заголовки и тело запроса.
* Response:
Описывает возможные ответы, их коды состояния и типы данных.

        📌Пример WADL:
        <application xmlns="http://wadl.dev.java.net/2009/02">
          <resources base="http://example.com/api">
            <resource path="/users">
              <method name="GET">
                <response>
                  <representation mediaType="application/json"/>
                </response>
              </method>
              <method name="POST">
                <request>
                  <representation mediaType="application/json"/>
                </request>
                <response>
                  <representation mediaType="application/json"/>
                </response>
              </method>
            </resource>
          </resources>
        </application>
Этот пример описывает ресурс /users с методами GET и POST. Такой документ помогает разработчикам понять структуру и поведение RESTful API, облегчая интеграцию и автоматизацию взаимодействия.

## 61.	 Что такое логи?
Логи (или журналы событий) — это записи о действиях, событиях и процессах, происходящих в системе, приложении или на сервере. Логи используются для отслеживания работы программ, диагностики ошибок, мониторинга безопасности и анализа поведения системы. Они представляют собой текстовые файлы или структурированные данные, которые записывают информацию о том, что и когда произошло.

## 62.	 Что такое логирование?
Логирование — это процесс записи событий, действий и состояний в систему или приложение с целью их последующего анализа, мониторинга и отладки. Логирование позволяет разработчикам и системным администраторам отслеживать, что происходит внутри программы или системы, фиксируя важные данные в логах (журналах).

## 63.	 Перечислите типы логов
### По источнику:
* Системные логи:
Записывают события операционной системы, такие как загрузка, завершение работы, ошибки драйверов и другие системные события.
* Приложенческие логи:
Создаются конкретными приложениями и фиксируют их работу, ошибки, запросы пользователей и другую важную информацию.
* Логи безопасности:
Содержат информацию о событиях, связанных с безопасностью, таких как попытки входа в систему, неудачные авторизации и другие действия пользователей.
* Сетевые логи:
Записывают события, связанные с сетевыми взаимодействиями, такие как соединения, передача данных и ошибки сети.
### По содержанию:
* Логи ошибок (Error logs):
Записывают сообщения об ошибках, которые произошли в системе или приложении. Эти логи помогают разработчикам и администраторам выявлять и устранять проблемы.
* Логи доступа (Access logs):
Фиксируют информацию о запросах, сделанных к ресурсам, включая IP-адреса, запрашиваемые URL, время запроса и статус ответа. Они полезны для анализа трафика и использования системы.
* Трассировочные логи (Trace logs):
Предназначены для детальной отладки приложения и содержат подробную информацию о последовательности выполнения кода. Полезны для понимания того, как выполняется программа.
* Логи аудита (Audit logs):
Записывают действия пользователей и изменения в системе для целей аудита и соответствия нормативным требованиям. Они важны для отслеживания несанкционированных действий.
### По уровню важности:
* DEBUG:
Подробная информация, полезная для отладки приложения. Используется в процессе разработки для выявления проблем.
* INFO:
Общая информация о нормальной работе приложения, полезная для понимания его состояния.
* WARNING:
Предупреждения о потенциальных проблемах, которые могут потребовать внимания, но не являются критическими.
* ERROR:
Сообщения об ошибках, которые произошли в приложении, но не привели к его завершению.
* CRITICAL:
Критические ошибки, которые могут привести к сбоям в работе приложения или системы.
### По способу хранения:
* Логи в текстовом формате:
Простые текстовые файлы, которые можно открыть и прочитать. Часто используются в системных и приложенческих логах.
* Структурированные логи:
Логи, представленные в формате, который легко анализировать, например, JSON или XML. Упрощает автоматизированный анализ и обработку данных.
* Двухуровневые логи:
Используют временные метки и уникальные идентификаторы для связи событий. Это позволяет лучше отслеживать и анализировать последовательности действий.

## 64.	 Механизм записи информации в логи
### Выбор библиотек для логирования
Для записи логов часто используются специализированные библиотеки или фреймворки, которые упрощают процесс. 
### Определение уровней логирования
Перед записью логов необходимо определить уровни важности (severity levels), такие как DEBUG, INFO, WARNING, ERROR и CRITICAL. Это помогает классифицировать сообщения и управлять объемом информации, фиксируемой в логах.
### Создание логгера
В большинстве библиотек нужно создать объект логгера, который будет использоваться для записи сообщений. Это может включать настройку формата сообщений, уровня важности и других параметров.
### Запись сообщений в логи
После настройки логгера, можно записывать сообщения с помощью методов, соответствующих уровням важности. Например:

    logger.debug("Это отладочное сообщение")
    logger.info("Это информационное сообщение")
### Выбор метода вывода логов
Логи могут выводиться различными способами:
* В файл: Логи записываются в текстовые файлы на диске. Это позволяет сохранять историю событий и проводить анализ в дальнейшем.
* На консоль: Логи выводятся в стандартный вывод (консоль), что полезно для разработки и тестирования.
* В удаленные системы: Логи могут отправляться на удаленные серверы или системы мониторинга для централизованного хранения и анализа.
### Ротация логов
Для предотвращения переполнения дискового пространства и управления объемом логов можно настроить ротацию логов. Это включает автоматическую архивацию и удаление старых логов. Многие библиотеки предоставляют встроенные механизмы для ротации.
### Форматирование логов
Формат сообщений в логах можно настраивать в зависимости от нужд. Например, можно включать временные метки, уровень важности, идентификаторы сессий и другие параметры, которые помогут в дальнейшем анализе.
### Обработка исключений
Логирование исключений также является важным аспектом. В случае возникновения ошибки можно использовать специальный метод для записи стека вызовов и деталей ошибки.

## 65.	 Зачем нужны логи? Какие инструменты знаете для сбора логов?
### Основные цели логов:
* Диагностика ошибок: Логи помогают разработчикам и системным администраторам выявлять и устранять ошибки. Когда происходит сбой или непредвиденное поведение, логи могут показать, что вызвало проблему.
* Мониторинг работы: Логи позволяют отслеживать состояние системы или приложения в реальном времени. Это помогает убедиться, что система работает корректно, и выявлять потенциальные проблемы.
* Безопасность: В логах фиксируются события, связанные с доступом к системе (вход в систему, неудачные попытки авторизации и т. д.), что важно для отслеживания подозрительных действий и обеспечения безопасности.
* Анализ производительности: Логи могут использоваться для анализа производительности приложений или системы — например, сколько времени заняло выполнение определенной операции.
* Аудит: Логи могут фиксировать важные события, такие как изменения в конфигурации или выполнение критических 
### Инструменты:
#### ELK-стек:
* Elasticsearch: Мощная поисковая и аналитическая система, которая позволяет индексировать и искать данные.
* Logstash: Инструмент для сбора, преобразования и отправки данных в Elasticsearch.
* Kibana: Веб-интерфейс для визуализации и анализа данных, хранящихся в Elasticsearch.
#### Graylog:
Платформа для централизованного управления логами, которая позволяет собирать, индексировать и анализировать данные. Graylog предоставляет мощные инструменты для визуализации и поиска.
#### Splunk:
Коммерческое решение для анализа логов и мониторинга, которое поддерживает широкий спектр источников данных и предлагает мощные функции поиска и отчетности.
##### Fluentd:
Инструмент для сбора и агрегации логов, который позволяет отправлять данные в различные базы данных и системы мониторинга.
#### Loggly:
Облачный сервис для анализа и мониторинга логов, который предлагает функции поиска и визуализации данных.
#### Papertrail:
Облачный сервис для сбора и управления логами, который позволяет легко просматривать и анализировать данные в реальном времени.
#### Promtail и Loki:
* Promtail: Инструмент для сбора логов, который работает с Grafana Loki.
* Loki: Система для хранения логов, разработанная для интеграции с Grafana, предлагающая простое хранение и поиск логов.
#### Sentry:
Инструмент для мониторинга и отслеживания ошибок в приложениях, который собирает информацию о сбоях и исключениях, а также предоставляет анализ.
#### Nagios:
Система мониторинга, которая может отслеживать журналы и уведомлять о проблемах, связанных с работой серверов и приложений.

## 66.	 Привести примеры, когда QA-специалисту необходимо собрать логи с помощью Kibana
* Отслеживание аномалий:
При обнаружении неожиданного поведения приложения, Kibana помогает визуализировать данные логов, чтобы выявить и понять причины аномалий. Это может быть особенно полезно при многократном воспроизведении проблемы.
* Проверка пользовательских сценариев:
После выпуска новой функциональности, QA-специалист использует Kibana для мониторинга логов и проверки, что ключевые пользовательские сценарии работают правильно и без ошибок.
* Анализ сбросов и исключений:
При сбоях в системе или возникновении исключений, логи помогают анализировать причины этих событий и определять, в каких условиях они происходят. Kibana позволяет легко фильтровать и просматривать эти записи.
* Производительность под нагрузкой:
В ходе нагрузочного тестирования приложение подвергается высокой нагрузке. QA-специалист использует Kibana для мониторинга производительности системы и выявления узких мест, таких как задержки или перегрузки.
* Анализ журналов безопасности:
Kibana помогает отслеживать и анализировать события безопасности, такие как попытки несанкционированного доступа или изменения прав доступа, чтобы обеспечить безопасность системы.
* Проверка обновлений и изменений:
После внедрения обновлений в систему, QA-специалист использует Kibana для мониторинга логов и удостоверения, что изменения не вызвали новых проблем или ошибок.

## 67.	 Что такое DNS?
DNS (Domain Name System) — это система, которая переводит читаемые человеком доменные имена (например, www.example.com) в IP-адреса (например, 192.0.2.1), которые используются компьютерами для связи в интернете.
### Основные компоненты DNS:
* Доменные имена: Читаемые человеком имена (например, www.example.com).
* IP-адреса: Числовые адреса, которые используются для идентификации устройств в сети (например, 192.0.2.1).
* DNS-серверы: Серверы, которые хранят информацию о доменных именах и IP-адресах и отвечают на запросы клиентов.
### Как работает DNS:
* Запрос: Когда вы вводите доменное имя в браузер, ваш компьютер отправляет запрос на DNS-сервер.
* Рекурсивный сервер: Этот сервер обращается к другим DNS-серверам, чтобы найти соответствующий IP-адрес.
* Авторитетные серверы: Эти серверы хранят информацию о конкретных доменных зонах и предоставляют конечный ответ.
* Ответ: После нахождения IP-адреса, ваш компьютер получает ответ и использует его для подключения к нужному серверу.

## 68.	 Что такое IP адрес?
IP-адрес (Internet Protocol Address) — это уникальный числовой идентификатор, присваиваемый каждому устройству, подключённому к сети Интернет или локальной сети. Он используется для идентификации и связи между устройствами.
### Основные виды IP-адресов:
#### IPv4:
* Состоит из четырех десятичных чисел (октетов), разделенных точками.

        📌Пример: 192.168.0.1
* Ограничен диапазоном от 0.0.0.0 до 255.255.255.255, что составляет около 4.3 миллиарда уникальных адресов.
* Постепенно заменяется IPv6 из-за нехватки адресов.
#### IPv6:
* Состоит из восьми групп шестнадцатеричных чисел, разделенных двоеточиями.

        📌Пример: 2001:0db8:85a3:0000:0000:8a2e:0370:7334
* Обеспечивает значительно больше адресов, чем IPv4 (примерно 3.4 x 10^38 уникальных адресов).
### Типы IP-адресов:
#### Публичный IP-адрес:
* Уникален в глобальном масштабе и используется для идентификации устройства в интернете.
* 📌Пример: IP-адрес веб-сервера, к которому обращаются пользователи.
#### Частный IP-адрес:
* Используется внутри локальных сетей и не уникален в глобальном масштабе.
* 📌Пример: IP-адреса устройств в домашней или офисной сети (например, 192.168.1.1).
##### Статический IP-адрес:
* Назначается устройству на постоянной основе и не изменяется.
* Используется для серверов, почтовых серверов и других служб, требующих постоянного IP-адреса.
#### Динамический IP-адрес:
* Временно присваивается устройству из пула доступных адресов.
* Обычно используется в домашних сетях и может изменяться при каждом подключении к сети.
### Зачем нужны IP-адреса:
* Идентификация: Позволяют уникально идентифицировать устройства в сети.
* Маршрутизация: Обеспечивают правильную маршрутизацию данных от источника к получателю.
* Безопасность: Могут использоваться для контроля доступа и блокировки нежелательного трафика.
### Как работает IP-адрес:
* Запрос DNS: Когда вы вводите URL в браузере, ваш запрос направляется на DNS-сервер, который переводит доменное имя в IP-адрес.
* Маршрутизация: Данные отправляются на IP-адрес получателя через сеть маршрутизаторов.
* Обратный процесс

## 69.	 Что такое порт?
Порт — это числовой идентификатор в сетевом протоколе, который используется для различения различных услуг или приложений на одном устройстве. В контексте IP-сетей порты помогают маршрутизировать запросы к нужным приложениям или службам на сервере или компьютере.
### Основные характеристики порта:
#### Числовой диапазон:
* Порты имеют значения от 0 до 65535.
* Порты от 0 до 1023 называются "зарезервированными" или "привилегированными" и используются для системных служб и известных протоколов.
#### Классификация портов:
* Зарезервированные порты (0-1023): Используются для системных служб и протоколов, таких как HTTP (порт 80), HTTPS (порт 443), FTP (порт 21), SSH (порт 22).
* Регистируемые порты (1024-49151): Присваиваются для пользовательских процессов и приложений.
* Динамические или частные порты (49152-65535): Используются временно для клиента приложений.
#### Зачем нужны порты:
* Разграничение услуг: Позволяют различным приложениям и службам на одном устройстве использовать сеть одновременно.
* Маршрутизация: Обеспечивают правильное направление сетевых пакетов к нужному приложению.
* Безопасность: Могут использоваться для фильтрации трафика и настройки брандмауэров.

## 70.	 Что такое хост?
Хост (host) — это устройство или компьютер, подключенный к сети, который предоставляет и получает сетевые ресурсы и услуги. В контексте Интернета хосты могут быть любыми устройствами, такими как серверы, рабочие станции, мобильные устройства, или даже умные бытовые приборы.
### Основные характеристики хоста:
#### Идентификация:
* Хосты имеют уникальные IP-адреса, которые идентифицируют их в сети.
* 📌Пример: Компьютер с IP-адресом 192.168.1.1.
#### Функциональность:
* Хосты могут предоставлять различные услуги, такие как веб-серверы, базы данных, почтовые серверы и т.д.
* 📌Пример: Веб-сервер, который обслуживает веб-страницы.
#### Сетевое взаимодействие:
* Хосты обмениваются данными и выполняют запросы друг друга.
* 📌Пример: Клиентское устройство запрашивает веб-страницу с хоста-сервера.
### Примеры хостов:
#### Сервер:
* Осуществляет обслуживание веб-страниц, хранение данных, обработку запросов и многое другое.
* 📌Пример: Веб-сервер, такой как Apache или Nginx.
#### Клиент:
* Пользовательское устройство, которое запрашивает и получает данные от серверов.
* 📌Пример: Ваш компьютер или смартфон при просмотре веб-страницы.
#### Мобильные устройства:
* Устройства, такие как смартфоны и планшеты, которые подключаются к сети и используют её ресурсы.
* 📌Пример: Мобильное приложение, которое взаимодействует с сервером для получения данных.
#### Умные устройства (IoT):
* Устройства, подключённые к Интернету для выполнения различных задач.
* 📌Пример: Умные термостаты, камеры наблюдения, умные холодильники.

## 71.	 Что такое URL
URL (Uniform Resource Locator) — это строка, используемая для обозначения адреса ресурса в интернете. Это понятный и стандартный способ указания местоположения веб-страниц, изображений, видео и других ресурсов.
### Основные компоненты URL:
#### Схема (Scheme):
* Указывает используемый протокол для доступа к ресурсу.
* 📌Пример: http, https, ftp.
* 📌Пример: https://www.example.com
#### Доменное имя (Domain name):
* Указывает адрес сервера, на котором размещен ресурс.
* 📌Пример: www.example.com
#### Путь (Path):
* Указывает местоположение ресурса на сервере.
* 📌Пример: /path/to/resource
#### Параметры (Query parameters):
* Дополнительные параметры, передаваемые серверу для обработки.
* 📌Пример: ?key1=value1&key2=value2
#### Фрагмент (Fragment):
* Указывает на определённый раздел или элемент внутри страницы.
* 📌Пример: #section1
        
        📌Пример полного URL:
        https://www.example.com/path/to/resource?key1=value1&key2=value2#section1
        Разделение URL на компоненты:
        * https: Схема, указывающая на использование защищенного HTTP.
        * www.example.com: Доменное имя, указывающее на сервер.
        * /path/to/resource: Путь к ресурсу на сервере.
        * ?key1=value1&key2=value2: Параметры запроса.
        * #section1: Фрагмент, указывающий на определённый раздел страницы.

## 72.	 В чем разница между URI, URL, URN ?
URI (Uniform Resource Identifier), URL (Uniform Resource Locator) и URN (Uniform Resource Name) — это понятия, связанные с обозначением ресурсов в интернете, но они имеют разные акценты и функции.
### URI (Uniform Resource Identifier):
* Определение: Общий термин, который включает в себя как URL, так и URN. URI представляет собой строку символов, используемую для идентификации ресурса.
* 📌Примеры: URL и URN являются подмножествами URI.
### URL (Uniform Resource Locator):
* Определение: Специфический тип URI, который не только идентифицирует ресурс, но и предоставляет средство его нахождения. URL содержит информацию о способе доступа к ресурсу (например, через HTTP, FTP) и его местоположении.
* 📌Пример: https://www.example.com/path/to/resource
### URN (Uniform Resource Name):
* Определение: Другой тип URI, который используется для идентификации ресурса по имени в рамках пространства имён. URN не включает в себя информацию о способе доступа к ресурсу или его местоположении.
* 📌Пример: urn:isbn:978-3-16-148410-0 (идентификатор для книги по её ISBN)
### Сравнение:
* URI: Общий термин, включающий в себя как URL, так и URN. Пример: https://www.example.com, urn:isbn:978-3-16-148410-0
* URL: Специфический тип URI, который указывает способ доступа и местоположение ресурса. Пример: https://www.example.com/path/to/resource
* URN: Специфический тип URI, который указывает уникальное имя ресурса без информации о доступе к нему. Пример: urn:isbn:978-3-16-148410-0

## 73.	 Из чего состоит url адрес?
### Основные компоненты URL:
#### Схема (Scheme):
* Указывает используемый протокол для доступа к ресурсу.
* 📌Пример: http, https, ftp.
* 📌Пример: https://www.example.com
#### Доменное имя (Domain name):
* Указывает адрес сервера, на котором размещен ресурс.
* 📌Пример: www.example.com
#### Путь (Path):
* Указывает местоположение ресурса на сервере.
* 📌Пример: /path/to/resource
#### Параметры (Query parameters):
* Дополнительные параметры, передаваемые серверу для обработки.
* 📌Пример: ?key1=value1&key2=value2
#### Фрагмент (Fragment):
* Указывает на определённый раздел или элемент внутри страницы.
* 📌Пример: #section1
        
        📌Пример полного URL:
        https://www.example.com/path/to/resource?key1=value1&key2=value2#section1
        Разделение URL на компоненты:
        * https: Схема, указывающая на использование защищенного HTTP.
        * www.example.com: Доменное имя, указывающее на сервер.
        * /path/to/resource: Путь к ресурсу на сервере.
        * ?key1=value1&key2=value2: Параметры запроса.
        * #section1: Фрагмент, указывающий на определённый раздел страницы.

## 74.	 Что происходит после ввода запроса в адресную строку?
#### Разрешение доменного имени
* DNS-запрос: Браузер проверяет, есть ли уже кешированный IP-адрес для введенного доменного имени. Если нет, он отправляет запрос к DNS-серверу (Domain Name System) для разрешения доменного имени в IP-адрес.
* Ответ от DNS: DNS-сервер возвращает IP-адрес, связанный с доменным именем. Например, если вы ввели www.example.com, DNS-сервер вернет его IP-адрес (например, 192.0.2.1).
#### Установка соединения
* TCP-соединение: После получения IP-адреса браузер устанавливает соединение с сервером, используя TCP (Transmission Control Protocol). Этот процесс включает:
* Трехстороннее рукопожатие: Браузер отправляет SYN-запрос на сервер, сервер отвечает SYN-ACK, и браузер завершает процесс, отправляя ACK.
* Безопасное соединение (при необходимости): Если используется HTTPS, происходит дополнительный этап — TLS/SSL-рукопожатие, где устанавливается зашифрованное соединение.
#### Отправка HTTP-запроса
* Формирование HTTP-запроса: Браузер формирует HTTP-запрос (или HTTPS-запрос) для запрашиваемого ресурса. Запрос включает:
* Метод (например, GET, POST)
* URL-адрес
* Заголовки (например, User-Agent, Accept)
* (для POST) тело запроса с данными
* Отправка запроса: HTTP-запрос отправляется на сервер по установленному соединению.
#### Обработка запроса сервером
* бработка запроса: Веб-сервер принимает HTTP-запрос и обрабатывает его. Это может включать:
* Поиск запрашиваемого ресурса (например, HTML-страницы, изображения, данные из базы данных).
* Выполнение бизнес-логики (например, обработка форм, аутентификация пользователей).
* Формирование ответа: Сервер формирует HTTP-ответ, который включает:
* Статус-код (например, 200 OK, 404 Not Found)
* Заголовки (например, Content-Type, Content-Length)
* Тело ответа с содержимым ресурса (например, HTML-код)
#### Получение и отображение ответа
* Получение ответа: Браузер получает HTTP-ответ от сервера.
* Отображение контента: Браузер обрабатывает ответ и отображает его на экране. Это включает:
* Интерпретацию HTML-кода, CSS и JavaScript.
* Загрузка дополнительных ресурсов, таких как изображения, скрипты и стили, если они указаны в HTML.
#### Кеширование и хранение данных
* Кеширование: Браузер может кешировать ресурсы (например, изображения, страницы) для ускорения загрузки при последующих запросах.
* Локальное хранилище: Если веб-приложение использует локальное хранилище (например, cookies, Local Storage), данные могут быть сохранены для использования в будущем.

## 75.	 Что такое HTTP?
HTTP (Hypertext Transfer Protocol) — это протокол, используемый для передачи данных в Интернете. Он является основой для передачи и получения информации в веб-приложениях и представляет собой стандартный способ общения между клиентом (например, веб-браузером) и сервером (например, веб-сервером).
### Основные характеристики HTTP:
* Протокол прикладного уровня: HTTP работает на прикладном уровне модели OSI и служит для передачи данных, таких как HTML-документы, изображения, видео и другие ресурсы.
* Клиент-серверная архитектура: Взаимодействие в HTTP происходит по модели "клиент-сервер". Клиент отправляет запросы к серверу, а сервер обрабатывает эти запросы и отправляет ответы обратно клиенту.
* Безсостояние (Stateless): Каждый HTTP-запрос обрабатывается независимо, без сохранения состояния между запросами. Это означает, что сервер не хранит информацию о предыдущих запросах от клиента. Это упрощает архитектуру, но для поддержания состояния используются другие механизмы, такие как cookies и сессии.
* етоды HTTP: HTTP поддерживает различные методы, которые определяют тип действия, которое нужно выполнить. 
* Статусы HTTP: Сервер отвечает на запросы с помощью статус-кодов, которые информируют клиента о результате обработки запроса. 
* Заголовки HTTP: Заголовки содержат метаданные о запросе или ответе, такие как тип контента, длина тела, настройки кэширования и т.д. 

## 76.	 Что такое HTTPS?
HTTPS (Hypertext Transfer Protocol Secure) — это расширение протокола HTTP, которое обеспечивает безопасную передачу данных в Интернете. HTTPS сочетает в себе протокол HTTP с протоколами шифрования, такими как TLS (Transport Layer Security) или SSL (Secure Sockets Layer), для защиты данных, передаваемых между клиентом (например, веб-браузером) и сервером.
### Основные характеристики HTTPS:
* Шифрование данных: При использовании HTTPS данные шифруются, что делает их недоступными для третьих лиц во время передачи. Это защищает конфиденциальную информацию, такую как пароли, номера кредитных карт и личные данные, от перехвата.
* Аутентификация: HTTPS обеспечивает проверку подлинности сервера. При установлении соединения браузер проверяет, что сервер действительно тот, за кого он себя выдает, путем проверки цифрового сертификата, выданного доверенным центром сертификации (CA). Это помогает предотвратить атаки типа "человек посередине" (MITM), когда злоумышленник может попытаться перехватить или изменить данные.
* Целостность данных: HTTPS гарантирует, что данные не были изменены или повреждены во время передачи. Если данные были модифицированы, соединение будет разорвано, и пользователь получит предупреждение.
* Порт: HTTPS по умолчанию использует порт 443, в отличие от HTTP, который использует порт 80.
### Принципы работы HTTPS
#### TLS/SSL-рукопожатие:
Когда браузер устанавливает HTTPS-соединение с сервером, происходит процесс, называемый "рукопожатие". В ходе этого процесса:
* Браузер запрашивает сертификат сервера.
* Сервер отправляет свой сертификат браузеру.
* Браузер проверяет сертификат у центра сертификации.
* Если сертификат действителен, устанавливается зашифрованное соединение.
#### Шифрование: 
После успешного рукопожатия данные между клиентом и сервером шифруются с использованием симметричного шифрования. Это означает, что один и тот же ключ используется для шифрования и расшифровки данных.

## 77.	 SSL и TLS - это?
SSL (Secure Sockets Layer) и TLS (Transport Layer Security) — это криптографические протоколы, предназначенные для обеспечения безопасности передачи данных в интернете.
### SSL (Secure Sockets Layer)
* Что это: Старший предшественник TLS, разрабатываемый компанией Netscape в середине 90-х.
* Версии: SSL 1.0, 2.0, 3.0 (последняя версия).
* Функции: Обеспечивает шифрование данных между клиентом и сервером, что защищает данные от перехвата и подделки.
### TLS (Transport Layer Security)
* Что это: Преемник SSL, который включает улучшенные алгоритмы шифрования и протоколы.
* Версии: TLS 1.0, 1.1, 1.2, 1.3 (последняя версия).
* Функции: Обеспечивает более безопасное и эффективное шифрование данных по сравнению с SSL, защищая данные от перехвата и подделки.
### Основные функции SSL и TLS:
* Шифрование: Преобразование данных в код, чтобы только авторизованные пользователи могли его прочитать.
* Аутентификация: Подтверждение подлинности участников обмена данными.
* Целостность данных: Защита данных от подделки и изменения в процессе передачи.
### Пример использования:
* HTTPS: Протокол HTTP поверх TLS (или SSL), используемый для защиты веб-сайтов.
        
        https://www.example.com
### Как это работает:
#### Рукопожатие (Handshake):
* Клиент и сервер договариваются об алгоритмах шифрования.
* Сервер отправляет клиенту свой сертификат для подтверждения подлинности.
#### Сеансовый ключ:
* Клиент и сервер создают и обмениваются сеансовыми ключами, которые используются для шифрования данных.
#### Шифрованное соединение:
* Все данные, передаваемые между клиентом и сервером, шифруются с использованием сеансовых ключей.
### Зачем это нужно:
* Конфиденциальность: Шифрование данных защищает их от перехвата.
* Аутентификация: Убедитесь, что вы общаетесь с подлинным сервером.
* Целостность: Гарантия, что данные не были изменены во время передачи.

## 78.	 TCP/IP - это?
TCP (Transmission Control Protocol) и UDP (User Datagram Protocol) — это два основных транспортных протокола в стеке TCP/IP, которые используются для передачи данных по сети.
### TCP (Transmission Control Protocol):
* Надежность: Обеспечивает гарантированную доставку данных. Если пакет потерян или поврежден, TCP запросит его повторную передачу.
* Порядок доставки: Обеспечивает доставку данных в правильном порядке.
* Соединение: Требует установления соединения между отправителем и получателем (трехфазное рукопожатие).
* Пример использования: Веб-браузеры (HTTP/HTTPS), электронная почта (SMTP), файловые передачи (FTP).
### Пример TCP-сессии:
* Установление соединения (трехфазное рукопожатие):
* Клиент отправляет SYN (Synchronize) пакет на сервер.
* Сервер отвечает SYN-ACK (Synchronize-Acknowledge).
* Клиент отправляет ACK (Acknowledge), завершив установление соединения.
* Передача данных: Данные передаются, и подтверждения (ACK) отправляются для каждого пакета.
* Завершение соединения: Обмен FIN (Finish) и ACK пакетами для закрытия соединения.
### UDP (User Datagram Protocol):
* Простота: Протокол без соединения, не требует установления соединения перед передачей данных.
* Быстрота: Быстрее TCP, так как не требует проверки доставки пакетов и установления соединения.
* Порядок доставки: Не гарантирует доставку данных в правильном порядке или вообще.
* Пример использования: Видеоконференции, стриминг мультимедиа, онлайн-игры.
### Пример использования UDP:
* Передача данных: Клиент отправляет данные (датаграммы) на сервер, не устанавливая соединение.
* Нет подтверждений: Данные передаются без подтверждений и гарантий доставки.
### Сравнение TCP и UDP:
* Надежность: TCP обеспечивает надежную передачу данных, UDP — нет.
* Порядок доставки: TCP гарантирует порядок доставки, UDP — нет.
* Производительность: UDP быстрее, так как не требует установления соединения и подтверждений.
* Использование: TCP используется для критически важных данных, UDP — для приложений, где важна скорость, а не надежность.

## 79.	 Уровни TCP/IP?
TCP/IP (Transmission Control Protocol/Internet Protocol) — это многослойный стек протоколов, который обеспечивает передачу данных в интернете. Он состоит из четырех основных уровней:
### Прикладной уровень (Application Layer):
* Включает протоколы, которые обеспечивают взаимодействие приложений.
* Примеры: HTTP (для веб-браузеров), FTP (для передачи файлов), SMTP (для электронной почты).
### Транспортный уровень (Transport Layer):
* Обеспечивает надежную передачу данных между устройствами.
* Основные протоколы: TCP (Transmission Control Protocol) и UDP (User Datagram Protocol).
* TCP: Гарантирует доставку данных в правильном порядке и без потерь.
* UDP: Предоставляет быструю, но ненадежную передачу данных без гарантий доставки.
### Сетевой уровень (Internet Layer):
* Отвечает за маршрутизацию данных по сети.
* Основной протокол: IP (Internet Protocol).
* Делится на две версии: IPv4 и IPv6.
* Маршрутизация пакетов данных между источником и получателем через сеть.
### Канальный уровень (Link Layer):
* Обеспечивает физическую передачу данных через различные сетевые среды.
* Включает в себя технологии, такие как Ethernet, Wi-Fi и другие канальные протоколы.
* Отвечает за передачу данных между соседними узлами в сети.

        📌Визуализация уровней TCP/IP:
         Прикладной уровень      (HTTP, FTP, SMTP)
        Транспортный уровень    (TCP, UDP)
        Сетевой уровень         (IP)
        Канальный уровень       (Ethernet, Wi-Fi)
## 80.	 Уровни OSI?
Модель OSI (Open Systems Interconnection) — это концептуальная модель, которая описывает и стандартизирует функции сетевых протоколов для обеспечения совместимости и взаимодействия различных сетевых систем. Она состоит из семи уровней:
### Физический уровень (Physical Layer):
* Ответственен за передачу сырых битов данных по физическим средам (кабели, оптоволокно, радиоволны).
* 📌Примеры: кабели, разъемы, электрические сигналы.
### Канальный уровень (Data Link Layer):
* Обеспечивает передачу данных между соседними узлами в сети и корректирует ошибки, возникающие на физическом уровне.
* 📌Примеры: Ethernet, MAC-адреса, PPP.
### Сетевой уровень (Network Layer):
* Отвечает за маршрутизацию и адресацию данных между различными сетями.
* 📌Примеры: IP-адресация, маршрутизаторы, протокол IP.
### Транспортный уровень (Transport Layer):
* Обеспечивает надежную передачу данных между узлами, контроль ошибок и управления потоком.
* 📌Примеры: TCP, UDP.
### Сеансовый уровень (Session Layer):
* Управляет установлением, поддержанием и завершением сеансов связи между приложениями.
* 📌Примеры: сеансы в протоколах RPC, NetBIOS.
### Уровень представления (Presentation Layer):
* Отвечает за преобразование данных в формат, удобный для приложения, включая шифрование и сжатие.
* 📌Примеры: SSL/TLS, форматирование данных (JPEG, ASCII).
### Прикладной уровень (Application Layer):
* Обеспечивает взаимодействие пользователя с приложением и предоставляет сетевые услуги пользователю.
* 📌Примеры: HTTP, FTP, SMTP, DNS.

        📌Визуализация уровней OSI:
        Прикладной уровень      (HTTP, FTP, SMTP)
        Уровень представления   (SSL/TLS, JPEG)
        Сеансовый уровень       (RPC, NetBIOS)
        Транспортный уровень    (TCP, UDP)
        Сетевой уровень         (IP)
        Канальный уровень       (Ethernet, MAC)
        Физический уровень      (кабели, электрические сигналы)
## 81.	 Что такое FTP?
FTP (File Transfer Protocol) — это стандартный сетевой протокол, используемый для передачи файлов между компьютерами по сети, такой как Интернет. FTP позволяет пользователям загружать файлы с удаленных серверов и загружать файлы на них.
### Основные характеристики FTP:
#### Клиент-серверная архитектура:
* FTP использует модель "клиент-сервер", где клиент подключается к FTP-серверу для обмена файлами.
#### Два соединения:
* Управляющее соединение: Устанавливается для передачи команд и управления сеансом.
* Данные соединение: Используется для передачи самих файлов.
#### Аутентификация:
* Пользователи могут входить в систему с использованием учетных данных (логин и пароль), хотя существует также анонимный доступ для публичных FTP-серверов.
#### Режимы передачи:
* Активный режим: Клиент открывает порт и ожидает подключения от сервера.
* Пассивный режим: Сервер открывает порт и сообщает клиенту, который затем подключается.
### Пример использования FTP:
#### Загрузка файла:
* Пользователь подключается к FTP-серверу и использует команду GET для загрузки файла на свой локальный компьютер.
#### Загрузка файла на сервер:
# Пользователь подключается к FTP-серверу и использует команду PUT для загрузки файла на сервер.
### Команды FTP:
* LIST: Список файлов и каталогов.
* RETR: Загрузка файла с сервера.
* STOR: Загрузка файла на сервер.
* DELE: Удаление файла с сервера.
* MKD: Создание нового каталога.
* RMD: Удаление каталога.

## 82.	Какую структуру имеет веб-страница?
### Структура веб-страницы
#### Doctype:
* Определяет тип документа и версию HTML.
* 📌Пример: <!DOCTYPE html>
#### HTML элемент:
* Корневой элемент, который заключает всю страницу.
* 📌Пример: <html>...</html>
#### Head элемент:
* Содержит метаинформацию о странице, такую как заголовки, стили, скрипты и метатеги.
* 📌Пример:

        <head>
          <meta charset="UTF-8">
          <title>Пример страницы</title>
          <link rel="stylesheet" href="styles.css">
        </head>
#### Body элемент:
* Содержит видимое содержание страницы: текст, изображения, ссылки, формы и другие элементы.
* 📌Пример:

        <body>
          <h1>Заголовок страницы</h1>
          <p>Текст страницы</p>
          <img src="image.jpg" alt="Пример изображения">
        </body>
### Основные элементы внутри Body
#### Заголовки (Headings):
* Определяют структуру документа и важность разделов.
* 📌Примеры: <h1>, <h2>, <h3> и т.д.
#### Параграфы (Paragraphs):
* Представляют блоки текста.
* 📌Пример: <p>Текст параграфа</p>
#### Ссылки (Links):
* Обеспечивают навигацию между страницами и ресурсами.
* 📌Пример: <a href="https://example.com">Ссылка</a>
#### Изображения (Images):
* Вставляют изображения в страницу.
* 📌Пример: <img src="image.jpg" alt="Описание изображения">
#### Списки (Lists):
* Представляют упорядоченные (нумерованные) или неупорядоченные (маркированные) списки.
* 📌Примеры: <ul>, <ol>, <li>
#### Формы (Forms):
* Позволяют пользователям вводить данные и отправлять их на сервер.
* 📌Пример:

        <form action="/submit" method="post">
          <label for="name">Имя:</label>
          <input type="text" id="name" name="name">
          <input type="submit" value="Отправить">
        </form>

## 83.	Что такое HTML/CSS/JavaScript?
### HTML (HyperText Markup Language):
* Описание: Основной язык разметки для создания структуры веб-страниц.
* Функция: Определяет элементы на странице, такие как заголовки, параграфы, изображения и ссылки.
* 📌Пример:

      <html>
        <head>
          <title>Пример страницы</title>
        </head>
        <body>
          <h1>Заголовок</h1>
          <p>Текст страницы</p>
        </body>
      </html>
### CSS (Cascading Style Sheets):
* Описание: Язык стилей, используемый для определения внешнего вида и оформления веб-страниц.
* Функция: Позволяет изменять шрифты, цвета, макеты и другие визуальные аспекты.
* 📌Пример:

      body {
        background-color: lightblue;
      }
      h1 {
        color: navy;
        text-align: center;
      }
      p {
        font-family: verdana;
        font-size: 14px;
      }
### JavaScript:
* Описание: Язык программирования, используемый для добавления интерактивности и динамического поведения на веб-страницах.
* Функция: Позволяет изменять контент страницы на основе пользовательских действий, отправлять данные на сервер и многое другое.
* 📌Пример:

      document.addEventListener('DOMContentLoaded', function() {
        document.querySelector('h1').textContent = 'Новый заголовок';
      });
### Как они работают вместе:
* HTML задаёт структуру страницы.
* CSS определяет её внешний вид.
* JavaScript добавляет интерактивность и динамическое поведение.

## 84.	Что такое AJAX?
AJAX (Asynchronous JavaScript and XML) — это технология для создания интерактивных веб-приложений. Она позволяет обновлять части веб-страницы без полной перезагрузки, делая взаимодействие более плавным и отзывчивым.
### Основные компоненты AJAX:
* Asynchronous (Асинхронный):
* Запросы отправляются и обрабатываются в фоновом режиме, не блокируя работу страницы.
* 📌Пример: Пользователь может продолжать работать с веб-страницей, пока данные загружаются на сервер.
### JavaScript:
* Язык программирования, который используется для создания и отправки запросов, а также обработки полученных данных.
* 📌Пример: XMLHttpRequest или Fetch API.
### XML (или JSON):
* Формат данных для обмена между клиентом и сервером. Хотя "XML" есть в названии, чаще используется JSON.
* 📌Пример: Получение и отправка данных в формате JSON.
### Как работает AJAX:
#### Создание запроса:
* JavaScript код создает запрос к серверу.
* 📌Пример:
        
        var xhr = new XMLHttpRequest();
        xhr.open("GET", "data.json", true);
        xhr.send();
#### Отправка запроса:
* Запрос отправляется серверу асинхронно, не мешая работе пользователя с веб-страницей.
#### Обработка ответа:
* Сервер обрабатывает запрос и отправляет ответ.
* 📌Пример: Получение данных с сервера в формате JSON.
#### Обновление страницы:
* JavaScript обновляет содержимое страницы на основе полученных данных без полной перезагрузки.
* 📌Пример:

        xhr.onreadystatechange = function() {
          if (xhr.readyState == 4 && xhr.status == 200) {
            var data = JSON.parse(xhr.responseText);
            document.getElementById("result").innerHTML = data.message;
          }
        };
### Преимущества AJAX:
* Быстрота и отзывчивость: Обновление частей страницы без перезагрузки.
* Удобство для пользователя: Более плавное взаимодействие и быстрый отклик.
* Эффективность: Сокращение объема передаваемых данных и улучшение производительности.

## 85.	Что такое адаптивная и респонсивная верстка?
Адаптивная и респонсивная верстка — это два подхода к созданию веб-страниц, которые выглядят и работают хорошо на различных устройствах и экранах. Несмотря на общую цель, они имеют разные подходы и характеристики.
### Адаптивная верстка (Adaptive Design):
#### Фиксированные точки перелома (breakpoints):
* Веб-страница адаптируется к заранее определенным размерам экранов.
* Используются различные макеты для разных устройств (например, десктоп, планшет, мобильный).
#### Несколько версий дизайна:
* Разработка отдельных макетов для каждого типа устройства.
* При загрузке страницы сервер или клиент выбирает наиболее подходящий макет.
#### Производительность:
* Чаще всего быстрее в разработке для конкретных устройств.
* Может быть менее гибкой при появлении новых размеров экранов или устройств.
#### 📌Примеры использования:
* Сайты с высокими требованиями к дизайну и контролю за пользовательским интерфейсом на каждом устройстве.
#### 📌Пример адаптивной верстки:
    
    @media screen and (max-width: 1200px) {
      /* Стили для десктопов */
    }
    @media screen and (max-width: 768px) {
      /* Стили для планшетов */
    }
    @media screen and (max-width: 480px) {
      /* Стили для мобильных устройств */
    }
### Респонсивная верстка (Responsive Design):
#### Гибкие сетки и макеты:
* Веб-страница динамически адаптируется к размеру экрана благодаря гибким сеткам, изображениям и медиазапросам.
* Используется один макет, который меняется в зависимости от размеров экрана.
#### Гибкость и универсальность:
* Страница автоматически подстраивается под любой размер экрана.
* Поддержка новых устройств без дополнительных настроек.
#### Производительность:
* Требует больше усилий при разработке, но обеспечивает более универсальное решение.
* Легче поддерживать и обновлять.
#### 📌Примеры использования:
* Современные сайты и приложения, стремящиеся к обеспечению хорошего пользовательского опыта на всех устройствах.
#### 📌Пример респонсивной верстки:

    .container {
      width: 100%;
      max-width: 1200px;
      margin: 0 auto;
    }
    
    img {
      max-width: 100%;
      height: auto;
    }
### Основные отличия:
* Адаптивная верстка: Использует фиксированные точки перелома и несколько макетов для разных устройств.
* Респонсивная верстка: Использует гибкие сетки и автоматически подстраивается под любые размеры экранов.

## 86.	Как протестировать адаптивную верстку?
### Тестирование в браузере:
* Инструменты разработчика: Используй встроенные инструменты разработчика в браузерах (например, Chrome DevTools). Они позволяют изменять размеры экрана и проверять, как отображается сайт.
* Эмуляция устройств: В инструментах разработчика можно эмулировать различные устройства, такие как смартфоны, планшеты и десктопы.
### Ручное тестирование на устройствах:
* Реальные устройства: Проверь сайт на различных реальных устройствах, таких как смартфоны, планшеты и десктопы.
* Операционные системы: Убедись, что сайт корректно отображается на разных операционных системах (iOS, Android, Windows, macOS).
### Инструменты для тестирования:
* BrowserStack или Sauce Labs: Эти инструменты позволяют тестировать сайты на различных устройствах и браузерах в облаке.
* Responsive Design Mode: В Firefox и Safari есть режимы для тестирования адаптивного дизайна.
### Тестирование на разных браузерах:
* Кроссбраузерное тестирование: Проверь сайт в разных браузерах, таких как Chrome, Firefox, Safari, Edge, чтобы убедиться, что адаптивная верстка работает везде.
### Тестирование под различными разрешениями экрана:
* Точки перелома: Измени размеры окна браузера вручную или используй предустановленные точки перелома в инструментах разработчика, чтобы проверить, как сайт выглядит при различных разрешениях.
### Использование автоматизированных тестов:
* Selenium или Cypress: Эти инструменты можно использовать для автоматизации тестов адаптивной верстки.
### Анализ и исправление:
* Отчеты и логи: Используй отчеты и логи для анализа проблем и их исправления.
* Отзыв пользователя: Учти обратную связь от пользователей, чтобы улучшить адаптивность и удобство использования сайта.

## 87.	Как выполнить Debug страницы в браузере?
### Основные шаги для отладки (Debugging) страницы в браузере:
#### Открытие инструментов разработчика:
* В большинстве браузеров можно открыть инструменты разработчика с помощью клавиши F12 или комбинации Ctrl+Shift+I (Cmd+Option+I на Mac).
#### Консоль (Console):
* Использование: Позволяет видеть ошибки JavaScript, выводить лог-сообщения и выполнять JavaScript-код.
* 📌Пример: В консоли можно написать console.log('Hello, world!'); для проверки вывода.
#### Элементы (Elements):
* Использование: Просмотр и редактирование HTML и CSS. Здесь можно проверить структуру страницы и стили для каждого элемента.
* 📌Пример: Выбрать элемент страницы и изменить его стиль, чтобы посмотреть, как изменения отразятся в реальном времени.
#### Источники (Sources):
* Использование: Просмотр и отладка исходного кода JavaScript. Можно устанавливать точки останова (breakpoints) для пошагового выполнения кода.
* 📌Пример: Установить точку останова на строку кода и выполнить пошаговую отладку.
#### Сеть (Network):
* Использование: Мониторинг сетевых запросов, отправляемых и получаемых страницей. Здесь можно увидеть все запросы к серверу, их время выполнения и статус.
* 📌Пример: Проверить загрузку ресурсов (картинок, скриптов, стилей) и выявить медленные запросы.
#### Производительность (Performance):
* Использование: Анализ производительности страницы, замеры времени загрузки и рендеринга.
* 📌Пример: Записать и проанализировать производительность, выявив узкие места, влияющие на скорость загрузки.
#### Память (Memory):
* Использование: Анализ использования памяти приложением, выявление утечек памяти.
* 📌Пример: Создать снимок (snapshot) памяти и проанализировать его.
#### События (Event Listeners):
* Использование: Просмотр и управление событиями, которые привязаны к элементам на странице.
* 📌Пример: Найти все события click, связанные с кнопкой, и проверить их работу.
#### Аудиты (Audits):
* Использование: Проведение аудита производительности, доступности и лучших практик для страницы с помощью инструментов, таких как Lighthouse.
* 📌Пример: Запустить аудит Lighthouse и получить рекомендации по улучшению производительности и доступности.

### 📌Пример отладки JavaScript:
#### Открытие консоли:
* Нажми F12 или Ctrl+Shift+I, перейди на вкладку "Console".
#### Поиск ошибок:
* Посмотри на сообщения об ошибках, которые отображаются в консоли. Ошибки обычно выделены красным цветом.
#### Исполнение кода:
* Введи и исполни простую команду, например, console.log('Hello, world!');.
#### Установка точки останова:
* Перейди на вкладку "Sources", найди нужный файл JavaScript, и установи точку останова, кликнув на номер строки.
#### Пошаговое выполнение:
 Обнови страницу, и когда выполнение дойдет до точки останова, используй кнопки для пошагового выполнения кода и анализа переменных.

## 88.	Что такое WebSocket и как проверить обрыв соединения?
WebSocket — это протокол связи, который обеспечивает постоянное соединение между клиентом и сервером для обмена данными в реальном времени. Он позволяет устанавливать двусторонний канал связи, что делает его идеальным для приложений, требующих мгновенной передачи данных, таких как чаты, игры и финансовые приложения.
### Основные характеристики WebSocket:
* Двусторонняя связь: Обе стороны (клиент и сервер) могут отправлять данные независимо друг от друга.
* Экономия ресурсов: После первоначального установления соединения обмен данными происходит с меньшими накладными расходами, чем при использовании HTTP.
* Состояние: WebSocket сохраняет состояние соединения, что позволяет передавать данные более эффективно.
### Как проверить обрыв соединения WebSocket?
Проверка обрыва соединения WebSocket может выполняться различными способами. Вот основные подходы:
#### Обработка событий
WebSocket API предоставляет события, которые можно использовать для отслеживания состояния соединения.
* onclose: Это событие срабатывает, когда соединение закрывается. Вы можете использовать его, чтобы выполнить определенные действия, такие как попытка переподключения.
*  onerror: Это событие срабатывает, когда возникает ошибка в соединении.
#### Периодические проверки
Вы можете настроить периодические проверки (ping/pong) для проверки активности соединения:
* Ping: Сервер периодически отправляет "ping" сообщения клиенту.
* Pong: Клиент отвечает "pong". Если сервер не получает ответ в течение заданного времени, он может считать соединение потерянным.
#### Проверка состояния соединения
* Проверяйте состояние соединения с помощью свойства readyState объекта WebSocket

## 89.	Как узнать схему API проекта
### Официальная документация:
Проверьте, есть ли у проекта официальная документация API. Обычно она включает описание эндпоинтов, параметров, методов запросов (GET, POST и т.д.), форматов ответов и ошибок.
### OpenAPI Specification (Swagger):
Если проект использует OpenAPI, вы можете найти файл спецификации (обычно openapi.yaml или swagger.json), который описывает все эндпоинты и их параметры.
### Инструменты разработки
* Postman: Если проект использует Postman для тестирования API, вы можете открыть коллекции API, которые содержат информацию о эндпоинтах, их параметрах и примерах запросов.
* Insomnia: Аналогично Postman, этот инструмент также может иметь коллекции с описанием API.
### Код проекта
* Исходный код: Если у вас есть доступ к исходному коду проекта, посмотрите на файлы, отвечающие за маршрутизацию (например, в Node.js это может быть файл с определением маршрутов). Обычно в этих файлах можно найти описание эндпоинтов и их логики.
* Комментарии и аннотации: Проверьте наличие комментариев в коде, которые могут содержать описание API.
### Инструменты для анализа API
* Swagger UI: Если проект использует Swagger, вы можете открыть интерфейс Swagger UI, который визуально представляет все доступные эндпоинты API и их параметры.
* GraphQL: Если API использует GraphQL, вы можете использовать инструменты, такие как GraphiQL или Apollo Studio, чтобы исследовать схему и доступные запросы.
### Запросы к API
* Тестирование через cURL или Postman: 
* Логи сервера: Если у вас есть доступ к логам сервера, они могут содержать информацию о запросах, которые поступали в API.

## 90.	Можно ли начать тестирование, если интерфейс (frontend) ещё не готов? Как?
### Тестирование API
Если backend уже готов, вы можете тестировать API. Это включает:
* Функциональное тестирование: Проверка, что API возвращает ожидаемые результаты для различных запросов.
* Тестирование эндпоинтов: Используйте инструменты, такие как Postman или cURL, для отправки запросов и проверки ответов.
* Тестирование производительности: Проверка времени отклика и нагрузки на API с помощью инструментов, таких как JMeter или Locust.
### Тестирование на уровне логики
* Юнит-тесты: Пишите юнит-тесты для отдельных функций или методов на backend. Это помогает убедиться, что логика приложения работает правильно.
* Интеграционные тесты: Проверяйте взаимодействие между различными модулями или компонентами.
### Создание моков (mock)
* Мок-серверы: Если frontend ещё не готов, можно использовать мок-серверы (например, Mockoon или JSON Server) для имитации ответов API. Это позволяет тестировать логику frontend, даже если сам интерфейс ещё не реализован.
### Тестирование пользовательского опыта (UX)
Если у вас есть прототипы или макеты интерфейса (например, созданные с помощью Figma или Adobe XD), вы можете проводить тесты на пользовательском опыте.
* Пользовательское тестирование: Попросите пользователей взаимодействовать с прототипами, чтобы получить обратную связь.
* Тестирование навигации: Оцените, насколько интуитивно понятен интерфейс, даже если он не реализован.
### Автоматизированное тестирование
Вы можете начать разрабатывать автоматизированные тесты, которые будут запускаться, когда интерфейс будет готов.
* Тесты на уровне API: Напишите тесты, которые будут проверять взаимодействие между фронтендом и бэкендом.
* Тесты производительности: Настройте сценарии для тестирования производительности.
### Непрерывная интеграция (CI)
Если у вас настроен CI/CD, интегрируйте тесты в процесс сборки, чтобы они автоматически запускались при каждом изменении кода.

## 91.	Какие виды тестирования можно применить только к Web?
* Тестирование совместимости: Проверка работы веб-приложения в разных браузерах, версиях и на различных устройствах (мобильных, планшетах и т.д.).
* Тестирование производительности: Оценка быстродействия, нагрузки и масштабируемости веб-приложения под различными условиями.
* Тестирование безопасности: Выявление уязвимостей, таких как SQL-инъекции, XSS (межсайтовый скриптинг) и другие угрозы, специфичные для веб-среды.
* Тестирование пользовательского интерфейса (UI): Оценка удобства и восприятия интерфейса, включая тестирование элементов, таких как кнопки, формы и навигация.
* Тестирование API: Проверка работы веб-сервисов и взаимодействия с ними через REST или SOAP.
* Тестирование адаптивности: Оценка, как веб-приложение отображается и функционирует на различных экранах и разрешениях.
* Тестирование локализации: Проверка корректности отображения контента на разных языках и соответствия культурным особенностям.
* Тестирование кросс-доменных запросов: Оценка обработки запросов, исходящих с других доменов, особенно в контексте безопасности.

## 92.	Каковы есть основные виды уязвимости веб-приложений?
* SQL-инъекции (SQLi): Позволяет злоумышленнику выполнять произвольные SQL-запросы, что может привести к утечке данных или изменениям в базе данных.
* Межсайтовый скриптинг (XSS): Позволяет вставлять вредоносные скрипты на страницы, которые будут выполнены в браузере пользователей.
* Переполнение буфера (Buffer Overflow): Уязвимость, возникающая при неправильной обработке данных, что может привести к исполнению произвольного кода.
* Уязвимости аутентификации и управления сессиями: Недостаточная защита паролей, использование слабых механизмов аутентификации или управление сессиями может привести к компрометации учетных записей.
* Неконтролируемый доступ к объектам: Позволяет пользователям получить доступ к ресурсам, на которые у них нет прав, например, к данным других пользователей.
* Ошибки конфигурации безопасности: Неправильная настройка серверов, баз данных или приложений может создать уязвимости.
* Межсайтовые запросы (CSRF): Позволяет злоумышленнику заставить пользователя выполнить нежелательное действие на сайте, где он аутентифицирован.
* Недостаточная защита данных: Хранение чувствительных данных (например, паролей) без шифрования или с использованием устаревших алгоритмов.
* Исполнение произвольного кода: Уязвимости, которые позволяют злоумышленнику исполнять произвольные команды или скрипты на сервере.
* Уязвимости в библиотеке и фреймворке: Использование устаревших или уязвимых сторонних библиотек и фреймворков может привести к компрометации безопасности приложения.

## 93.	Какие инструменты для тестирования Web performance client-side знаете?
* Google PageSpeed Insights: Оценка производительности страниц, включая рекомендации по улучшению скорости загрузки.
* Lighthouse: Инструмент от Google для аудита производительности, доступности и SEO веб-страниц. Входит в состав Chrome DevTools.
* GTmetrix: Анализирует производительность страницы и предоставляет детальные отчеты с рекомендациями по оптимизации.
* WebPageTest: Позволяет проводить тесты производительности с разных локаций и браузеров, предоставляет подробные результаты.
* Pingdom: Мониторинг скорости загрузки веб-сайта с отчетами о времени загрузки и производительности.
* New Relic: Обширный инструмент для мониторинга производительности приложений, включая клиентскую часть.
* Dynatrace: Инструмент для мониторинга и анализа производительности, включая работу с клиентской стороны.
* BrowserStack: Позволяет тестировать производительность веб-приложений на различных устройствах и браузерах.
* Fiddler: Инструмент для отладки HTTP-трафика, позволяет анализировать производительность запросов и ответов.
* JMeter (с поддержкой браузеров): Основной инструмент для нагрузочного тестирования, который можно адаптировать для тестирования клиентской производительности.
